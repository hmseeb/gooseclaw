---
phase: 20-infrastructure-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/gateway.py
  - docker/tests/test_hardening.py
autonomous: true
requirements: [HARD-03]

must_haves:
  truths:
    - "Container shutdown completes within 5 seconds even if cleanup hangs"
    - "Watchdog timer force-exits the process after grace period"
    - "Normal shutdown cancels the watchdog and exits cleanly"
  artifacts:
    - path: "docker/gateway.py"
      provides: "Shutdown handler with threading.Timer watchdog"
      contains: "threading.Timer"
    - path: "docker/tests/test_hardening.py"
      provides: "Tests for shutdown watchdog behavior"
      contains: "test_shutdown"
  key_links:
    - from: "docker/gateway.py shutdown()"
      to: "threading.Timer"
      via: "Watchdog started before cleanup, cancelled after"
      pattern: "watchdog.*Timer.*5\\.0"
    - from: "docker/gateway.py shutdown()"
      to: "os._exit"
      via: "Force exit on timeout"
      pattern: "os\\._exit"
---

<objective>
Add a 5-second watchdog timer to the gateway shutdown handler that force-kills the process if graceful cleanup hangs.

Purpose: Prevent container from hanging indefinitely on Railway restarts/deploys.
Output: Updated shutdown handler in gateway.py with watchdog, plus tests.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-infrastructure-hardening/20-RESEARCH.md
@docker/gateway.py (lines 9887-9910 shutdown handler)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add watchdog timer to shutdown handler</name>
  <files>docker/gateway.py</files>
  <action>
1. Read the shutdown handler at gateway.py lines 9887-9910.

2. Modify the `shutdown(_sig, _frame)` function to add a watchdog timer at the very start, before any cleanup:

   ```python
   def shutdown(_sig, _frame):
       global _job_engine_running, _cron_scheduler_running

       # Watchdog: force-exit after 5 seconds if cleanup hangs
       def _force_exit():
           print("[gateway] shutdown timeout exceeded, forcing exit", flush=True)
           os._exit(1)
       watchdog = threading.Timer(5.0, _force_exit)
       watchdog.daemon = True
       watchdog.start()

       # ... existing cleanup code unchanged ...

       watchdog.cancel()
       print("[gateway] shutdown complete")
   ```

3. Ensure `import threading` is already present at the top of gateway.py (it likely is, verify). Ensure `import os` is present too.

4. The watchdog.cancel() call goes AFTER all existing cleanup but BEFORE the final print. If there's already a final print or sys.exit() at the end of the handler, place cancel() just before it.

5. Do NOT change the existing cleanup logic (server.shutdown, _unload_channel, stop_goosed, _bot_manager.stop_all, etc.). Only wrap it with the watchdog.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && grep -A 15 "def shutdown" docker/gateway.py | grep -c "Timer\|_force_exit\|watchdog"</automated>
    <manual>Shutdown handler has watchdog timer wrapping existing cleanup</manual>
  </verify>
  <done>Shutdown handler starts a 5-second threading.Timer watchdog before cleanup, cancels it after cleanup, and calls os._exit(1) if timeout fires.</done>
</task>

<task type="auto">
  <name>Task 2: Add shutdown watchdog tests</name>
  <files>docker/tests/test_hardening.py</files>
  <action>
1. Add tests to `docker/tests/test_hardening.py` (append to existing file if plan 20-01 created it, or create it if it doesn't exist yet):

   - `test_shutdown_handler_has_watchdog(gateway_source)`: Inspect gateway.py source to verify the shutdown function contains `threading.Timer` and `os._exit`
   - `test_shutdown_watchdog_timeout_is_5_seconds(gateway_source)`: Inspect source for `Timer(5.0` or `Timer(5,` in the shutdown function
   - `test_shutdown_watchdog_is_daemon(gateway_source)`: Verify `watchdog.daemon = True` in the shutdown function (daemon thread so it doesn't prevent exit)
   - `test_shutdown_watchdog_cancelled_on_success(gateway_source)`: Verify `watchdog.cancel()` appears in the shutdown function

   Use the `gateway_source` fixture from conftest.py to read the source as a string.
   Extract just the shutdown function body for targeted assertions.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python -m pytest docker/tests/test_hardening.py -k "shutdown" -x -q --timeout=30</automated>
  </verify>
  <done>4 shutdown watchdog tests pass, validating timer presence, 5-second timeout, daemon flag, and cancel-on-success.</done>
</task>

</tasks>

<verification>
- gateway.py shutdown handler contains threading.Timer watchdog
- Watchdog fires os._exit(1) after 5 seconds
- Watchdog is cancelled on successful cleanup
- All shutdown tests pass
</verification>

<success_criteria>
- Shutdown handler wraps all cleanup with a 5-second watchdog timer
- Force-exit via os._exit(1) if timeout fires (not sys.exit which can be caught)
- Watchdog is a daemon thread (won't prevent normal exit)
- Normal shutdown path cancels the watchdog
- 4 tests validate the above
</success_criteria>

<output>
After completion, create `.planning/phases/20-infrastructure-hardening/20-02-SUMMARY.md`
</output>
