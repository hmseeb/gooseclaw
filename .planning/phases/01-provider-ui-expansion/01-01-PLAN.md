---
phase: 01-provider-ui-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/setup.html
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
  - UX-01
  - UX-02

must_haves:
  truths:
    - "User sees 15+ providers organized into Cloud API, Subscription, Local, Enterprise, and Custom categories"
    - "Each provider card displays name, description, pricing hint, and a clickable get-API-key link"
    - "All 8 new providers are present: mistral, xai, deepseek, together, cerebras, perplexity, ollama, azure-openai"
    - "After selecting any provider, the credentials step shows correct field(s) with format hints and help link"
  artifacts:
    - path: "docker/setup.html"
      provides: "Provider data registry and categorized grid UI"
      contains: "PROVIDERS"
  key_links:
    - from: "PROVIDERS data object"
      to: "provider-grid DOM rendering"
      via: "renderProviderGrid function"
      pattern: "renderProviderGrid|PROVIDERS"
    - from: "provider selection"
      to: "credential fields"
      via: "buildCredFields reads from PROVIDERS registry"
      pattern: "buildCredFields.*PROVIDERS"
---

<objective>
Define all 15+ providers as a structured JS data registry and render them in a categorized card grid on step 0, with rich card content (name, description, pricing hint, get-key link). Update the credentials step to dynamically build fields from the registry for all providers including the 8 new ones.

Purpose: Foundation for the expanded wizard -- all provider metadata lives in one place, grid is data-driven, and credentials work for every provider.
Output: Updated docker/setup.html with provider registry, categorized grid, and dynamic credential fields.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docker/setup.html
@docker/gateway.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create provider data registry and render categorized card grid</name>
  <files>docker/setup.html</files>
  <action>
In the script section of docker/setup.html, replace the hardcoded provider cards with a data-driven approach:

1. **Define a PROVIDERS object** at the top of the script block with ALL providers. Each entry keyed by provider ID (matching data-provider values used in gateway.py). Structure per provider:

```js
const PROVIDERS = {
  anthropic: {
    name: 'Anthropic',
    icon: '\u25B3',       // triangle
    desc: 'Claude models via API',
    category: 'cloud',    // cloud | subscription | local | enterprise | custom
    pricing: 'Pay per token',
    keyUrl: 'https://console.anthropic.com',
    envVar: 'ANTHROPIC_API_KEY',
    defaultModel: 'claude-sonnet-4-5',
    models: ['claude-sonnet-4-5', 'claude-sonnet-4-20250514', 'claude-opus-4', 'claude-haiku-3-5'],
    keyPlaceholder: 'sk-ant-api03-...',
    keyPrefix: 'sk-ant-',
  },
  // ... etc for all providers
};
```

Full provider list with data (use the reference table from planning context):

| ID | Name | Icon | Desc | Category | Pricing | Key URL | Env Var | Default Model | Key Placeholder | Models (top 3-4) |
|---|---|---|---|---|---|---|---|---|---|---|
| anthropic | Anthropic | triangle | Claude models via API | cloud | Pay per token | console.anthropic.com | ANTHROPIC_API_KEY | claude-sonnet-4-5 | sk-ant-api03-... | claude-sonnet-4-5, claude-opus-4, claude-haiku-3-5 |
| openai | OpenAI | circle | GPT and o-series models | cloud | Pay per token | platform.openai.com/api-keys | OPENAI_API_KEY | gpt-4o | sk-... | gpt-4o, gpt-4o-mini, o3, o3-mini |
| google | Google AI | diamond | Gemini models | cloud | Free tier available | aistudio.google.com/apikey | GOOGLE_API_KEY | gemini-2.5-flash | AI... | gemini-2.5-flash, gemini-2.5-pro, gemini-2.0-flash |
| groq | Groq | lightning | Ultra-fast inference | cloud | Free tier available | console.groq.com/keys | GROQ_API_KEY | llama-3.3-70b-versatile | gsk_... | llama-3.3-70b-versatile, llama-3.1-8b-instant, mixtral-8x7b-32768 |
| openrouter | OpenRouter | arrows | Multi-model routing | cloud | Pay per token | openrouter.ai/keys | OPENROUTER_API_KEY | anthropic/claude-sonnet-4-5 | sk-or-... | anthropic/claude-sonnet-4-5, openai/gpt-4o, google/gemini-2.5-flash |
| mistral | Mistral | wind/M | European AI models | cloud | Pay per token | console.mistral.ai/api-keys | MISTRAL_API_KEY | mistral-medium-latest | ... | mistral-medium-latest, mistral-large-latest, mistral-small-latest |
| xai | xAI | star | Grok models | cloud | Pay per token | console.x.ai | XAI_API_KEY | grok-3 | xai-... | grok-3, grok-3-mini, grok-2 |
| deepseek | DeepSeek | wave | Cost-efficient reasoning | cloud | Pay per token | platform.deepseek.com/api_keys | DEEPSEEK_API_KEY | deepseek-chat | sk-... | deepseek-chat, deepseek-reasoner |
| together | Together AI | grid | Open-source model hub | cloud | Pay per token | api.together.ai/settings/api-keys | TOGETHER_API_KEY | meta-llama/Llama-3-70b-chat-hf | ... | meta-llama/Llama-3-70b-chat-hf, mistralai/Mixtral-8x7B-Instruct-v0.1 |
| cerebras | Cerebras | chip | Fastest inference | cloud | Free tier available | cloud.cerebras.ai | CEREBRAS_API_KEY | llama-3.3-70b | csk-... | llama-3.3-70b, llama-3.1-8b |
| perplexity | Perplexity | search | Search-augmented AI | cloud | Pay per token | perplexity.ai/settings/api | PERPLEXITY_API_KEY | sonar-pro | pplx-... | sonar-pro, sonar |
| claude-code | Claude Code | terminal | Use your Claude subscription | subscription | Included in plan | claude.ai/settings | CLAUDE_CODE_OAUTH_TOKEN | default | sk-ant-oat01-... | (none - uses subscription default) |
| ollama | Ollama | box | Run models locally | local | Free (self-hosted) | ollama.com/download | OLLAMA_HOST | (user models) | http://host.docker.internal:11434 | (user must pre-pull) |
| azure-openai | Azure OpenAI | cloud | Enterprise OpenAI | enterprise | Enterprise pricing | portal.azure.com | AZURE_OPENAI_API_KEY | (deployment name) | ... | (user deployment) |
| custom | Custom Endpoint | gear | Any OpenAI-compatible API | custom | Varies | N/A | (user-defined) | (user-defined) | https://... | (user-defined) |

Use simple unicode characters for icons (keep it consistent with existing style). Categories: cloud, subscription, local, enterprise, custom.

2. **Define category metadata:**
```js
const CATEGORIES = {
  cloud: { label: 'Cloud API', desc: 'API key providers' },
  subscription: { label: 'Subscription', desc: 'Use your existing plan' },
  local: { label: 'Local', desc: 'Self-hosted models' },
  enterprise: { label: 'Enterprise', desc: 'Corporate deployments' },
  custom: { label: 'Custom', desc: 'Bring your own endpoint' },
};
```

3. **Replace the static provider-grid HTML** in step-0 with a single `<div id="providerGrid"></div>` container.

4. **Create a `renderProviderGrid()` function** that:
   - Groups providers by category
   - For each category, renders a category header (label text, styled as a subtle section divider -- use existing step-title-like styling but smaller, e.g., `font-size: 11px; text-transform: uppercase; letter-spacing: 1.5px; color: var(--text-tertiary); margin: 20px 0 10px; font-family: 'JetBrains Mono', monospace;`)
   - Then renders a 2-column grid of provider cards
   - Each card shows: icon (top-left), name, description, pricing hint (small text below desc), and a small "Get key" link (opens in new tab, styled as subtle text-accent link, `font-size: 11px`)
   - Ollama card should NOT show "Get key" but instead "Download" linking to ollama.com/download
   - Custom card should NOT show any external link
   - Cards use `onclick="selectProvider('${id}')"` pattern
   - Call `renderProviderGrid()` on page load (right after PROVIDERS definition)

5. **Add CSS for category headers and updated cards:**
   - `.category-label` for the section headers
   - `.card-meta` for the pricing hint line (below desc)
   - `.card-link` for the "Get key" link (positioned bottom-right or below pricing, small, subtle)
   - Update `.provider-card` stagger animations to handle more cards (add nth-child rules up to 15, or use a JS-based approach: set `animation-delay` inline via the render function, e.g., `style="animation-delay: ${index * 0.04}s"`)

6. **Update `selectProvider()` function** to accept a string ID instead of a DOM element:
```js
function selectProvider(id) {
  document.querySelectorAll('.provider-card').forEach(c => c.classList.remove('selected'));
  const card = document.querySelector(`[data-provider="${id}"]`);
  if (card) card.classList.add('selected');
  selectedProvider = id;
  buildCredFields();
}
```

7. **Preserve the existing reconfigure logic** that auto-selects the previous provider on load. Update it to call `selectProvider(providerType)` with a string.

Do NOT change gateway.py in this task. Do NOT change the success screen or save logic yet.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -c "
import re
html = open('docker/setup.html').read()
# Check PROVIDERS object exists
assert 'const PROVIDERS' in html, 'PROVIDERS registry missing'
# Check all 15 providers are defined
providers = ['anthropic','openai','google','groq','openrouter','mistral','xai','deepseek','together','cerebras','perplexity','claude-code','ollama','azure-openai','custom']
for p in providers:
    assert p in html, f'Provider {p} missing from registry'
# Check categories
assert 'CATEGORIES' in html or 'category' in html, 'Categories missing'
# Check renderProviderGrid function
assert 'renderProviderGrid' in html, 'renderProviderGrid function missing'
# Check cards have pricing and key URL references
assert 'pricing' in html, 'Pricing hints missing'
assert 'keyUrl' in html or 'key_url' in html, 'Key URLs missing'
# Check provider count >= 15
count = len(re.findall(r'data-provider=', html))
assert count >= 15, f'Only {count} provider cards rendered (need 15+)'
print('All checks passed')
"</automated>
    <manual>Open docker/setup.html in browser. Verify categorized grid with 15+ providers, each showing name, description, pricing, and get-key link.</manual>
  </verify>
  <done>
    - PROVIDERS data registry defines 15 providers with name, icon, desc, category, pricing, keyUrl, envVar, defaultModel, models, keyPlaceholder
    - Provider grid renders cards grouped by category (Cloud API, Subscription, Local, Enterprise, Custom)
    - Each card shows name, description, pricing hint, and clickable key link
    - selectProvider works with string IDs
    - Reconfigure flow still auto-selects previous provider
  </done>
</task>

<task type="auto">
  <name>Task 2: Dynamic credential fields for all providers with format hints and help links</name>
  <files>docker/setup.html</files>
  <action>
Rewrite the `buildCredFields()` function to be fully data-driven from the PROVIDERS registry:

1. **For standard API key providers** (anthropic, openai, google, groq, openrouter, mistral, xai, deepseek, together, cerebras, perplexity):
   - Read `PROVIDERS[selectedProvider]` for name, keyPlaceholder, keyUrl, envVar
   - Render a single API key field with:
     - Label: `${provider.name} API Key`
     - Placeholder from registry
     - A help link below: `Get your API key at <a href="${provider.keyUrl}" target="_blank" rel="noopener">${provider.keyUrl}</a>` styled with `field-hint` class, link in `var(--text-accent)` color
     - Format hint if keyPrefix exists: `Key should start with <code>${provider.keyPrefix}</code>`
   - Input id: `apiKey` (consistent with existing save logic)

2. **For claude-code provider:**
   - Label: "Claude Setup Token"
   - Placeholder: "sk-ant-oat01-..."
   - Help text: "Run <code>claude setup-token</code> in your local terminal to get this token. Requires an active Claude Pro/Team subscription."
   - Link to claude.ai/settings
   - Input id: `claudeToken` (matches existing save logic)

3. **For ollama provider:**
   - Label: "Ollama Host URL"
   - Placeholder: "http://host.docker.internal:11434"
   - Default value pre-filled: "http://host.docker.internal:11434"
   - Help text: "Ollama must be running and accessible from this container. If running on the Docker host, use <code>host.docker.internal</code>."
   - Note: "Models must be pre-pulled locally with <code>ollama pull &lt;model&gt;</code>"
   - Input id: `ollamaHost`

4. **For azure-openai provider:**
   - Two fields:
     - "Azure OpenAI API Key" (input id: `azureKey`, placeholder: "...")
     - "Azure OpenAI Endpoint" (input id: `azureEndpoint`, placeholder: "https://your-resource.openai.azure.com")
   - Help text: "Find these in Azure Portal > your OpenAI resource > Keys and Endpoint"
   - Link to portal.azure.com

5. **For custom provider** (keep existing 3-field layout):
   - API Endpoint URL (id: `customUrl`)
   - Model Name (id: `customModel`, default: "gpt-4")
   - API Key (id: `customKey`, optional)
   - Keep existing field hints

6. **Ensure the validate button still works** for existing providers. For new providers (mistral, xai, deepseek, together, cerebras, perplexity), the validate endpoint in gateway.py does not yet have handlers -- that is Phase 2 work. The Test button should still be present but that is fine since the gateway returns a generic "Validation not available" for unknown providers.

7. **Ensure saveConfig() still works.** The save function reads field IDs to build the config payload. Update saveConfig() to handle the new providers:
   - For ollama: `config.ollama_host = document.getElementById('ollamaHost')?.value`
   - For azure-openai: `config.azure_key = document.getElementById('azureKey')?.value; config.azure_endpoint = document.getElementById('azureEndpoint')?.value`
   - Standard API key providers (including new ones) already use `config.api_key` which works

Do NOT modify gateway.py's env_map or validation logic -- that is Phase 2.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -c "
html = open('docker/setup.html').read()
# Check buildCredFields reads from PROVIDERS
assert 'PROVIDERS[' in html or 'PROVIDERS.' in html, 'buildCredFields not data-driven'
# Check credential fields for new providers
assert 'ollamaHost' in html or 'ollama_host' in html, 'Ollama host field missing'
assert 'azureKey' in html or 'azure_key' in html, 'Azure key field missing'
assert 'azureEndpoint' in html or 'azure_endpoint' in html, 'Azure endpoint field missing'
# Check help links exist (target=_blank pattern)
assert 'target=\"_blank\"' in html or \"target='_blank'\" in html, 'No external help links'
# Check format hints
assert 'keyPlaceholder' in html or 'placeholder' in html, 'Placeholders defined'
# Check saveConfig handles new providers
assert 'ollama' in html.split('saveConfig')[1] if 'saveConfig' in html else True, 'saveConfig missing ollama handling'
print('All checks passed')
"</automated>
    <manual>Select each provider in the wizard and verify correct credential fields appear with format hints and help links.</manual>
  </verify>
  <done>
    - buildCredFields() is data-driven from PROVIDERS registry for all 15 providers
    - Each provider shows correct input fields with appropriate placeholders
    - Help links to get API keys are shown for each provider
    - Format hints shown where key prefix patterns are known
    - Ollama shows host URL field with pre-filled default and pre-pull note
    - Azure shows dual fields (key + endpoint)
    - saveConfig() handles all new provider field IDs
    - Existing validate/save flow is not broken
  </done>
</task>

</tasks>

<verification>
Open docker/setup.html directly in a browser (file:// protocol works since it is self-contained). Verify:
1. Step 0 shows 15+ providers in categorized sections (Cloud API at top with ~11 providers, then Subscription, Local, Enterprise, Custom)
2. Each card has name, description, pricing hint text, and a "Get key" link
3. Click each provider -- correct credential fields appear on step 1
4. New providers (mistral, xai, etc.) show API key field with correct placeholder and help link
5. Ollama shows host URL field, Azure shows key + endpoint fields
6. Selecting a provider and clicking Continue still navigates correctly
</verification>

<success_criteria>
- 15+ providers rendered from PROVIDERS data registry
- Cards grouped by 5 categories with section headers
- Each card shows name, description, pricing, get-key link
- Credential fields are data-driven for all providers
- Existing functionality (select, validate, save) is not broken
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-ui-expansion/01-01-SUMMARY.md`
</output>
