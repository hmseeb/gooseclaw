---
phase: 01-provider-ui-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - docker/setup.html
autonomous: false
requirements:
  - MODL-01
  - MODL-02
  - MODL-03
  - MODL-04
  - UX-03
  - UX-04
  - UX-05
  - TG-01

must_haves:
  truths:
    - "After selecting a provider, user sees a dedicated model selection step with the default model pre-filled and a suggestions dropdown"
    - "Ollama model step shows a note that models must be pre-pulled locally"
    - "OpenRouter model step shows a note about multi-model routing"
    - "User can progress through 5 visible steps: Provider -> Credentials -> Model -> Settings -> Confirm"
    - "Settings step shows BotFather instructions for Telegram bot creation"
    - "Confirmation step shows a summary of provider, model, timezone, and whether Telegram is configured"
  artifacts:
    - path: "docker/setup.html"
      provides: "5-step wizard flow with model selection, settings, and confirmation summary"
      contains: "step-4"
  key_links:
    - from: "progress bar"
      to: "5 step indicators"
      via: "goToStep handles indices 0-4 plus success"
      pattern: "pnum-4|pstep-4"
    - from: "model step"
      to: "PROVIDERS[selectedProvider].models"
      via: "datalist populated from provider registry"
      pattern: "datalist|models"
    - from: "confirmation summary"
      to: "all form field values"
      via: "buildSummary reads selected config"
      pattern: "buildSummary|summary"
---

<objective>
Expand the wizard from 3 steps to 5 steps: Provider (0), Credentials (1), Model Selection (2), Optional Settings (3), Confirmation Summary (4). Add a dedicated model selection step with smart defaults and datalist suggestions. Add BotFather instructions to the Telegram field. Add a confirmation summary step before save.

Purpose: Users get a guided, clear flow where model selection is prominent (not buried in "options") and they can review everything before committing.
Output: Updated docker/setup.html with 5-step flow, model selection with suggestions, and confirmation summary.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-provider-ui-expansion/01-01-SUMMARY.md
@docker/setup.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand to 5-step flow with model selection, settings, and confirmation</name>
  <files>docker/setup.html</files>
  <action>
Restructure setup.html from 3 steps to 5 steps. This is a significant restructuring of the HTML and JS.

**A. Update the progress bar HTML** to show 5 steps instead of 3:

Steps: Provider (0) -> Credentials (1) -> Model (2) -> Settings (3) -> Confirm (4)

Add the additional progress-step and progress-line elements:
- pstep-0: Provider, pline-0
- pstep-1: Credentials, pline-1
- pstep-2: Model, pline-2
- pstep-3: Settings, pline-3
- pstep-4: Confirm (no trailing line)

Each with corresponding pnum-N and progress-label. Keep the existing styling.

**B. Update `goToStep()` function** to handle indices 0-4 instead of 0-2:
- Loop from 0 to 4 (inclusive) for progress indicators
- Step "success" still exists as a special case
- When navigating to step 2 (model), call `buildModelStep()` to populate model suggestions
- When navigating to step 4 (confirm), call `buildSummary()` to populate the summary

**C. Create step-2 (Model Selection) HTML:**

```html
<div class="step" id="step-2">
  <div class="step-title">Choose Model</div>
  <div id="model-info"></div>
  <div class="field">
    <label>Model</label>
    <input type="text" id="model" list="modelSuggestions" placeholder="loading...">
    <datalist id="modelSuggestions"></datalist>
    <div class="field-hint" id="modelHint"></div>
  </div>
  <div id="modelNotes"></div>
  <div class="btn-row">
    <button class="btn btn-ghost" onclick="goToStep(1)">Back</button>
    <button class="btn btn-primary" onclick="goToStep(3)">Continue</button>
  </div>
</div>
```

**D. Create `buildModelStep()` function:**
- Reads `PROVIDERS[selectedProvider]`
- Sets the model input value to `provider.defaultModel` if not already set by user
- Populates the datalist with `provider.models` array
- Sets `modelHint` to: `Default: <code>${provider.defaultModel}</code>` (or "Uses your subscription default" for claude-code)
- Adds provider-specific notes in `modelNotes`:
  - **Ollama**: "Models must be pre-pulled on your Ollama host with <code>ollama pull modelname</code> before they can be used here." (styled as an info-banner, similar to reconfig-banner styling but always visible)
  - **OpenRouter**: "OpenRouter routes to many providers. Model names use <code>provider/model</code> format. See <a href='https://openrouter.ai/models' target='_blank'>available models</a>."
  - **claude-code**: "Claude Code uses your subscription's default model. Model selection is not applicable."
  - **custom**: "Enter the exact model name your endpoint expects."
  - **azure-openai**: "Enter your Azure deployment name (not the base model name)."
  - For all others: empty
- Shows the default model prominently -- the input should be pre-filled, and the field hint shows "Default: <code>...</code>" with a note "change only if you know what you want"

**E. Restructure existing step-2 (Options) into step-3 (Settings):**

Move the existing "Optional Settings" content to step-3. Changes:
- Remove the Model Override field (it is now on step-2)
- Keep: Timezone, Telegram Bot Token, Web Auth Token
- Update Telegram field to include BotFather instructions:
  - Below the input, add a collapsible/always-visible instruction block:
    ```
    How to create a Telegram bot:
    1. Open Telegram and search for @BotFather
    2. Send /newbot and follow the prompts
    3. Copy the bot token (format: 123456:ABC-DEF...)
    4. Paste it above
    ```
  - Style this as a subtle instructional block (use `field-hint` styling but with numbered steps, slightly more prominent -- maybe a light border-left with accent color)
- Update Back button to go to step 2, Continue to go to step 4
- Remove the "Save & Start Agent" button from this step (it moves to step 4)

**F. Create step-4 (Confirmation Summary) HTML:**

```html
<div class="step" id="step-4">
  <div class="step-title">Review Configuration</div>
  <div id="configSummary"></div>
  <div class="btn-row">
    <button class="btn btn-ghost" onclick="goToStep(3)">Back</button>
    <button class="btn btn-primary" id="saveBtn" onclick="saveConfig()">
      Save &amp; Start Agent
      <svg ...arrow icon...></svg>
    </button>
  </div>
</div>
```

**G. Create `buildSummary()` function:**
- Reads all form values and builds a summary display
- Shows a card-style summary with rows:
  - Provider: `${PROVIDERS[selectedProvider].name}` with icon
  - Model: value from #model input (or "default" for claude-code)
  - Timezone: value from #timezone
  - Telegram: "Configured" or "Not configured"
  - Auth Token: "Custom" or "Auto-generated"
- Style each row as a subtle key-value pair:
  ```css
  .summary-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid var(--border-subtle); }
  .summary-key { font-size: 13px; color: var(--text-secondary); }
  .summary-val { font-size: 13px; color: var(--text-primary); font-weight: 500; }
  ```
- Wrap all rows in a container with the `bg-card` styling and rounded corners

**H. Update saveConfig():**
- Move the save button reference to step-4's saveBtn (same ID, just different location)
- The function itself does not need major changes -- it already reads from the correct element IDs
- Ensure the model field is read from the step-2 input (id="model" -- same as before, just in a different step)

**I. Fix step-0's Continue button** to still go to step 1 (no change needed, just verify).

**J. CSS additions:**
- `.model-note` for provider-specific notes (info banner style: left border accent, subtle background, padding 12px 16px, font-size 13px, margin-top 16px)
- `.summary-container`, `.summary-row`, `.summary-key`, `.summary-val` for the confirmation summary
- `.botfather-instructions` for the Telegram setup guide (ordered list with subtle styling)
- Update progress bar to handle 5 items gracefully at the container width of 540px (the labels may need to be shorter or hidden on smaller widths -- check that the existing responsive rule `.progress-label { display: none; }` at 480px is sufficient)

**K. Ensure the step-success screen** still works -- goToStep('success') should still show step-success and hide the progress bar or show all steps as done.

Do NOT modify gateway.py. Do NOT modify entrypoint.sh.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -c "
html = open('docker/setup.html').read()
# Check 5 steps exist
assert 'id=\"step-0\"' in html, 'step-0 missing'
assert 'id=\"step-1\"' in html, 'step-1 missing'
assert 'id=\"step-2\"' in html, 'step-2 missing'
assert 'id=\"step-3\"' in html, 'step-3 missing'
assert 'id=\"step-4\"' in html, 'step-4 missing'
assert 'id=\"step-success\"' in html, 'step-success missing'
# Check 5 progress indicators
assert 'pnum-4' in html, 'Progress step 4 missing'
assert 'pstep-4' in html, 'Progress step 4 container missing'
# Check model step elements
assert 'modelSuggestions' in html or 'datalist' in html, 'Model datalist missing'
assert 'buildModelStep' in html, 'buildModelStep function missing'
# Check summary step
assert 'buildSummary' in html, 'buildSummary function missing'
assert 'configSummary' in html or 'summary' in html.lower(), 'Summary container missing'
# Check BotFather instructions
assert 'BotFather' in html or 'botfather' in html.lower(), 'BotFather instructions missing'
# Check model notes for ollama and openrouter
assert 'pre-pull' in html.lower() or 'ollama pull' in html, 'Ollama model note missing'
assert 'openrouter' in html.lower() and ('route' in html.lower() or 'routing' in html.lower()), 'OpenRouter note missing'
# Verify goToStep handles 5 steps
import re
goto_match = re.search(r'for.*let i.*[;].*i\s*[<]=?\s*(\d)', html)
if goto_match:
    assert int(goto_match.group(1)) >= 4, f'goToStep loop only goes to {goto_match.group(1)}, need 4'
print('All checks passed')
"</automated>
    <manual>Open setup.html in browser. Walk through all 5 steps with any provider. Verify model suggestions appear, BotFather instructions visible, summary shows correct values.</manual>
  </verify>
  <done>
    - Progress bar shows 5 steps: Provider, Credentials, Model, Settings, Confirm
    - Model step (2) pre-fills default model and shows datalist with suggestions from PROVIDERS
    - Ollama model step shows "must pre-pull" note
    - OpenRouter model step shows multi-model routing note
    - Settings step (3) includes BotFather instructions for Telegram
    - Confirmation step (4) shows summary of all configured values
    - Save button is on the confirmation step
    - Navigation (back/forward) works across all 5 steps
    - Success screen still shows after save
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete 5-step wizard flow</name>
  <files>docker/setup.html</files>
  <action>Human verification of the complete 5-step wizard UI. All implementation is done in Task 1 -- this checkpoint verifies visual correctness and interactive flow that automated tests cannot fully capture.</action>
  <verify>
    <automated>echo "Checkpoint: manual verification required"</automated>
    <manual>
    1. Open docker/setup.html directly in a browser (file:// works since self-contained)
    2. Verify step 0: Providers are grouped into categories (Cloud API should have ~11 providers, then Subscription, Local, Enterprise, Custom sections). Each card shows name, description, pricing hint, and a "Get key" link.
    3. Select "Anthropic" -- verify step 1 shows API key field with placeholder "sk-ant-api03-..." and link to console.anthropic.com
    4. Select "Ollama" -- verify step 1 shows Host URL field pre-filled with host.docker.internal address
    5. Click Continue to step 2 (Model) -- verify default model is pre-filled and datalist shows suggestions
    6. For Ollama, verify the "must pre-pull" note appears on step 2
    7. For OpenRouter, verify the multi-model routing note appears
    8. Continue to step 3 (Settings) -- verify Telegram field has BotFather instructions
    9. Continue to step 4 (Confirm) -- verify summary shows provider, model, timezone, telegram status
    10. Verify "Save and Start Agent" button is on step 4 (not step 3)
    11. Verify Back buttons navigate correctly through all steps
    </manual>
  </verify>
  <done>User has visually verified the complete 5-step wizard flow works correctly with multiple providers, model suggestions, BotFather instructions, and confirmation summary.</done>
</task>

</tasks>

<verification>
Complete walkthrough of the 5-step wizard with multiple providers:
1. Test with Anthropic (cloud API key provider)
2. Test with Ollama (local, special fields)
3. Test with Azure OpenAI (enterprise, dual fields)
4. Test with Custom (3-field layout)
5. Verify all navigation, model suggestions, and summary rendering
</verification>

<success_criteria>
- 5 clearly labeled progress steps visible at all times
- Model selection step shows default model pre-filled with datalist dropdown
- Ollama and OpenRouter have provider-specific model notes
- BotFather instructions visible on settings step
- Confirmation summary accurately reflects all configured values
- Save button only on final confirmation step
- Full forward/back navigation works without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-ui-expansion/01-02-SUMMARY.md`
</output>
