---
phase: 07-channel-plugin-parity
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - docker/gateway.py
  - docker/test_gateway.py
autonomous: true
requirements: [CHAN-01, CHAN-03]

must_haves:
  truths:
    - "ChannelRelay.__call__ intercepts /help, /stop, /clear, /compact before relay"
    - "Command handlers use ctx['channel'] and ctx['channel_state'] instead of hardcoded _telegram_state"
    - "ChannelRelay tracks active relay via its own ChannelState, /stop kills it"
    - "Telegram backward compat: handlers default to _telegram_state when no channel_state in ctx"
  artifacts:
    - path: "docker/test_gateway.py"
      provides: "TestChannelRelayCommands, TestGeneralizedCommandHandlers, TestChannelRelayStop classes"
      contains: "class TestChannelRelayCommands"
    - path: "docker/gateway.py"
      provides: "Generalized command handlers and ChannelRelay with command interception + active relay tracking"
      contains: "ctx.get(\"channel_state\""
  key_links:
    - from: "ChannelRelay.__call__"
      to: "_command_router.dispatch"
      via: "command interception before relay"
      pattern: "_command_router\\.dispatch\\(text.*ctx\\)"
    - from: "_handle_cmd_stop"
      to: "ctx['channel_state']"
      via: "pop_active_relay from ctx state"
      pattern: "state\\.pop_active_relay"
    - from: "_handle_cmd_clear"
      to: "ctx['channel_state']"
      via: "kill_relay from ctx state"
      pattern: "state\\.kill_relay"
---

<objective>
Generalize the 4 command handlers to work for ANY channel (not just telegram) and give ChannelRelay command interception + active relay tracking so /stop works on channel plugins.

Purpose: CHAN-01 (commands work on any channel) and CHAN-03 (cancel via /stop) are the core parity features. Without these, channel plugins are relay-only with no user control.

Output: Generalized command handlers + ChannelRelay with command interception and active relay tracking. All existing telegram tests still pass.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-channel-plugin-parity/07-RESEARCH.md
@.planning/phases/06-shared-infrastructure-extraction/06-03-SUMMARY.md
@docker/gateway.py
@docker/test_gateway.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED -- Write failing tests for generalized commands and ChannelRelay command interception</name>
  <files>docker/test_gateway.py</files>
  <action>
Add three new test classes at the end of test_gateway.py. All tests must FAIL initially (the code they test does not exist yet).

**TestGeneralizedCommandHandlers** (CHAN-01 -- handler generalization):
- `test_stop_uses_ctx_channel_state`: Create a ChannelState, set an active relay on it, call `_handle_cmd_stop` with ctx containing `channel_state=that_state` and `channel="slack"`. Assert the relay was popped from THAT state (not _telegram_state). Assert send_fn was called with "Stopped."
- `test_stop_falls_back_to_telegram_state`: Call `_handle_cmd_stop` with ctx that has NO channel_state key. Assert it falls back to `_telegram_state.pop_active_relay`. (backward compat)
- `test_clear_uses_ctx_channel_and_state`: Create a ChannelState, set an active relay. Call `_handle_cmd_clear` with ctx containing `channel="slack"`, `channel_state=that_state`. Assert `state.kill_relay` was called. Assert `_session_manager.pop("slack", chat_id)` was called (not "telegram").
- `test_clear_falls_back_to_telegram`: Call `_handle_cmd_clear` with ctx that has no channel_state. Assert it falls back to _telegram_state and "telegram".
- `test_compact_uses_ctx_channel`: Call `_handle_cmd_compact` with ctx containing `channel="slack"`. Mock `_relay_to_goose_web`. Assert it was called with `channel="slack"` (not "telegram"). Assert `_session_manager.get("slack", chat_id)` was used.
- `test_help_works_any_channel`: Call `_handle_cmd_help` with ctx containing `channel="slack"`. Assert send_fn was called with help text (no channel-specific behavior, just verifying it doesn't crash).

**TestChannelRelayCommands** (CHAN-01 -- command interception):
- `test_relay_intercepts_help`: Create a ChannelRelay("test_ch"). Mock `_command_router.dispatch` to return True. Call `relay("user1", "/help", send_fn)`. Assert dispatch was called. Assert NO relay to goose web happened.
- `test_relay_intercepts_stop`: Same pattern with "/stop".
- `test_relay_intercepts_clear`: Same pattern with "/clear".
- `test_relay_passes_correct_ctx`: Create a ChannelRelay("test_ch"). Mock dispatch. Call `relay("user1", "/help", mock_send)`. Capture the ctx dict passed to dispatch. Assert `ctx["channel"] == "test_ch"`, `ctx["user_id"] == "user1"`, `ctx["send_fn"] is mock_send`, `ctx["channel_state"]` is a ChannelState instance.
- `test_relay_unknown_command_sends_error`: Create a ChannelRelay("test_ch"). Do NOT mock dispatch (use real router which won't have "/foo"). Call `relay("user1", "/foo", mock_send)`. Assert mock_send was called with text containing "Unknown command".
- `test_relay_non_command_still_relays`: Create a ChannelRelay("test_ch"). Mock `_relay_to_goose_web` (or `_do_ws_relay`). Call `relay("user1", "hello", mock_send)`. Assert relay function WAS called (regular text is not intercepted).
- `test_relay_command_returns_empty_string`: Call `relay("user1", "/help", mock_send)`. Assert return value is "".

**TestChannelRelayStop** (CHAN-03 -- active relay tracking + /stop cancel):
- `test_relay_sets_active_relay`: Create a ChannelRelay("test_ch"). Access its `_state`. Mock `_relay_to_goose_web` to capture sock_ref. Call `relay("user1", "hello")`. Assert `_state.set_active_relay` was called with user_id and a sock_ref list.
- `test_relay_pops_active_relay_after_complete`: Create a ChannelRelay("test_ch"). Mock relay. Call `relay("user1", "hello")`. Assert `_state.pop_active_relay("user1")` returns None (already popped in finally block).
- `test_stop_kills_channel_relay`: Create a ChannelRelay("test_ch"). Set up a mock active relay on its `_state`. Call `_handle_cmd_stop` with ctx pointing to that state. Assert the socket was closed and cancel event was set.
- `test_relay_respects_cancelled_flag`: Create a ChannelRelay. Mock `_relay_to_goose_web` to set `cancelled.set()` during execution (simulating /stop). Assert the relay returns "" and send_fn was NOT called with response text.

Use the existing test patterns from TestCommandRouter and TestChannelState as reference. Use `unittest.mock.patch` for gateway module globals. Each test class should have setUp/tearDown that cleans up `_session_manager._sessions` and any test state.

After writing: run `python3 -m unittest discover -s docker -p 'test_*.py' 2>&1` and verify the new tests FAIL (existing 134 still pass).
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -m unittest discover -s docker -p 'test_*.py' 2>&1 | tail -5</automated>
    <manual>New tests fail (expected), existing 134 tests pass</manual>
  </verify>
  <done>Three test classes exist with 17+ failing tests covering CHAN-01 (generalized handlers, command interception) and CHAN-03 (active relay tracking, /stop cancel). All pre-existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- Generalize command handlers and add command interception + relay tracking to ChannelRelay</name>
  <files>docker/gateway.py</files>
  <action>
Make all new tests pass with minimal implementation changes in gateway.py.

**Generalize command handlers** (lines ~3423-3495):

1. `_handle_cmd_stop` (line 3434): Replace `_telegram_state.pop_active_relay(chat_id)` with `state = ctx.get("channel_state", _telegram_state); sock_ref = state.pop_active_relay(chat_id)`. Replace `_session_manager.get("telegram", chat_id)` with `_session_manager.get(ctx.get("channel", "telegram"), chat_id)`. Replace the print `[telegram]` with `[{channel}]`.

2. `_handle_cmd_clear` (line 3460): Replace `_clear_chat(chat_id)` with inline logic that uses `ctx.get("channel_state", _telegram_state)` and `ctx.get("channel", "telegram")`:
   - `state = ctx.get("channel_state", _telegram_state)`
   - `channel = ctx.get("channel", "telegram")`
   - `state.kill_relay(chat_key)`
   - `old = _session_manager.pop(channel, chat_key)`
   - Keep calling `_restart_goose_and_prewarm(chat_id)` in a thread (goose web restart affects all channels, documented limitation)
   - Update print to use `[{channel}]` instead of `[telegram]`

3. `_handle_cmd_compact` (line 3473): Replace `_get_session_id(chat_id)` with `_session_manager.get(ctx.get("channel", "telegram"), chat_id)`. If no session exists, send error "No active session. Send a message first." and return. Replace `channel="telegram"` in `_relay_to_goose_web` call with `channel=ctx.get("channel", "telegram")`. For typing, call `ctx.get("channel_state", _telegram_state)` pattern if needed, but mainly just fix the channel routing.

4. `_handle_cmd_help`: No changes needed (already channel-agnostic, just calls `_command_router.get_help_text()`).

5. Keep `_clear_chat()` function as-is for backward compat (telegram poll loop might still call it directly). But the /clear HANDLER now uses generalized logic.

**ChannelRelay changes** (line 2914):

1. Add `self._state = ChannelState()` in `__init__`. This gives each ChannelRelay its own per-channel concurrency state.

2. Rewrite `__call__` to add command interception and active relay tracking:
   ```
   def __call__(self, user_id, text, send_fn=None):
       user_key = str(user_id)

       # Command interception (CHAN-01)
       if text and text.strip().startswith("/"):
           ctx = {
               "channel": self._name,
               "user_id": user_key,
               "send_fn": send_fn or (lambda t: None),
               "channel_state": self._state,
           }
           if _command_router.is_command(text):
               _command_router.dispatch(text, ctx)
               return ""
           # unknown command
           if send_fn:
               send_fn(f"Unknown command: {text.split()[0]}\nSend /help for available commands.")
           return ""

       # Session management (existing logic)
       session_id = _session_manager.get(self._name, user_key)
       if not session_id:
           session_id = f"{self._name}_{user_key}_{time.strftime('%Y%m%d_%H%M%S')}"
           _session_manager.set(self._name, user_key, session_id)

       # Active relay tracking (CHAN-03)
       cancelled = threading.Event()
       sock_ref = [None, cancelled]
       self._state.set_active_relay(user_key, sock_ref)

       try:
           # Streaming params (existing logic)
           setup = load_setup()
           verbosity = get_verbosity_for_channel(setup, self._name) if setup else "balanced"
           use_streaming = send_fn and verbosity != "quiet"

           if use_streaming:
               response_text, error = _relay_to_goose_web(
                   text, session_id, chat_id=user_key, channel=self._name,
                   flush_cb=send_fn, verbosity=verbosity,
                   sock_ref=sock_ref, flush_interval=2.0,
               )
           else:
               response_text, error = _relay_to_goose_web(
                   text, session_id, chat_id=user_key, channel=self._name,
                   sock_ref=sock_ref,
               )

           if cancelled.is_set():
               return ""

           if error:
               # retry with new session (existing logic)
               session_id = f"{self._name}_{user_key}_{time.strftime('%Y%m%d_%H%M%S')}"
               _session_manager.set(self._name, user_key, session_id)
               if use_streaming:
                   response_text, error = _relay_to_goose_web(
                       text, session_id, chat_id=user_key, channel=self._name,
                       flush_cb=send_fn, verbosity=verbosity,
                       sock_ref=sock_ref, flush_interval=2.0,
                   )
               else:
                   response_text, error = _relay_to_goose_web(
                       text, session_id, chat_id=user_key, channel=self._name,
                       sock_ref=sock_ref,
                   )

           if cancelled.is_set():
               return ""
           if error:
               return f"Error: {error}"
           return response_text
       finally:
           self._state.pop_active_relay(user_key)
   ```

   Key differences from current ChannelRelay:
   - Command interception block at the top
   - `sock_ref` passed to `_relay_to_goose_web` for /stop cancellation
   - `self._state.set_active_relay` / `pop_active_relay` for relay tracking
   - `cancelled` event check after relay
   - Uses `_relay_to_goose_web` directly instead of the old `_do_ws_relay` / `_do_ws_relay_streaming` (since `_relay_to_goose_web` already dispatches to the right relay function internally)

After writing: run full test suite. All 134 existing + all new tests must pass.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -m unittest discover -s docker -p 'test_*.py' 2>&1 | tail -5</automated>
    <manual>All tests pass including new ones for generalized commands and relay tracking</manual>
  </verify>
  <done>Command handlers use ctx["channel"] and ctx["channel_state"] with backward-compatible defaults. ChannelRelay intercepts commands before relay and tracks active relays for /stop. All tests green.</done>
</task>

</tasks>

<verification>
- `python3 -m unittest discover -s docker -p 'test_*.py' 2>&1` -- all pass (134 existing + ~17 new)
- `python3 -c "import docker.gateway as g; r = g.ChannelRelay('test'); print(hasattr(r, '_state'))"` -- True
- `grep -c 'ctx.get("channel_state"' docker/gateway.py` -- 3+ (stop, clear, compact handlers)
- `grep -c '_command_router.dispatch' docker/gateway.py` -- 2+ (ChannelRelay + anywhere else)
</verification>

<success_criteria>
- ChannelRelay("slack") user sends /help -> gets help text via send_fn, no relay to goose
- ChannelRelay("slack") user sends /stop -> kills active relay on the slack ChannelState, not _telegram_state
- ChannelRelay("slack") user sends /clear -> clears "slack" session via _session_manager, not "telegram"
- ChannelRelay("slack") user sends "hello" -> relays to goose web normally with sock_ref tracking
- Telegram commands still work identically (backward compat defaults)
- All 134+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-channel-plugin-parity/07-01-SUMMARY.md`
</output>
