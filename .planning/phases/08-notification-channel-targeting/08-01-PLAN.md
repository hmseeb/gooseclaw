---
phase: 08-notification-channel-targeting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/gateway.py
  - docker/scripts/remind.sh
  - docker/test_gateway.py
autonomous: true
requirements:
  - CHAN-07
  - CHAN-08
  - CHAN-09

must_haves:
  truths:
    - "POST /api/notify with channel parameter delivers only to that channel"
    - "POST /api/notify without channel parameter broadcasts to all channels (backward compatible)"
    - "Cron scheduler passes notify_channel from schedule.json job to notify_all"
    - "remind.sh accepts --notify-channel flag and includes it in the job payload"
  artifacts:
    - path: "docker/gateway.py"
      provides: "handle_notify passes channel param, _fire_cron_job passes notify_channel"
      contains: "notify_all(text, channel="
    - path: "docker/scripts/remind.sh"
      provides: "--notify-channel flag support"
      contains: "--notify-channel"
    - path: "docker/test_gateway.py"
      provides: "Tests for channel targeting"
      contains: "test_notify_with_channel"
  key_links:
    - from: "handle_notify"
      to: "notify_all"
      via: "channel parameter passthrough"
      pattern: "notify_all\\(text, channel="
    - from: "_fire_cron_job"
      to: "notify_all"
      via: "job.get notify_channel"
      pattern: "channel=job\\.get.*notify_channel"
    - from: "remind.sh cmd_create"
      to: "POST /api/jobs"
      via: "notify_channel in JSON payload"
      pattern: "notify_channel"
---

<objective>
Wire channel targeting through the entire notification pipeline: API endpoint, cron scheduler, and remind.sh CLI.

Purpose: All three notification paths (API, cron, remind.sh) can deliver to a specific channel instead of broadcasting to all. The plumbing (notify_all with channel param, job engine notify_channel field) already exists -- this plan connects the remaining gaps.

Output: Fully working per-channel notification targeting across all delivery paths, with tests.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-notification-channel-targeting/08-RESEARCH.md
@docker/gateway.py (handle_notify ~line 5735, _fire_cron_job ~line 2727, notify_all ~line 864)
@docker/scripts/remind.sh (cmd_create function)
@docker/scripts/job.sh (reference: --notify-channel flag implementation)
@docker/test_gateway.py (existing test patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire channel parameter through API and cron scheduler + tests</name>
  <files>docker/gateway.py, docker/test_gateway.py</files>
  <action>
**CHAN-07: handle_notify channel passthrough**

In `handle_notify()` (~line 5735), after extracting `text` from `data`, also extract the optional `channel` parameter:

```python
channel = data.get("channel")
if channel:
    channel = _sanitize_string(channel, max_length=100)
```

Then pass it to notify_all:

```python
result = notify_all(text, channel=channel)
```

This is backward compatible: if `channel` is not in the request body, it defaults to None and notify_all broadcasts to all (existing behavior).

**CHAN-08: _fire_cron_job channel passthrough**

In `_fire_cron_job()` (~line 2727), update BOTH notify_all calls to pass the job's notify_channel:

Line ~2760 (error case):
```python
notify_all(f"[cron:{job_id}] failed: {error}", channel=job.get("notify_channel"))
```

Line ~2770 (success output):
```python
notify_all(formatted, channel=job.get("notify_channel"))
```

**Tests (add to docker/test_gateway.py):**

1. `TestNotifyChannelTargeting` class:
   - `test_notify_with_channel_passes_to_notify_all`: Mock notify_all, call handle_notify with `{"text": "hello", "channel": "telegram"}`, verify notify_all called with `channel="telegram"`.
   - `test_notify_without_channel_broadcasts`: Mock notify_all, call handle_notify with `{"text": "hello"}` (no channel field), verify notify_all called with `channel=None`.
   - `test_notify_channel_sanitized`: Verify channel value is sanitized (stripped, length-limited).

2. `TestCronNotifyChannel` class (extend existing cron tests):
   - `test_cron_job_passes_notify_channel`: Mock _do_ws_relay to return ("output", None), mock notify_all, call _fire_cron_job with a job dict that has `notify_channel: "telegram"`, verify notify_all called with `channel="telegram"`.
   - `test_cron_job_error_passes_notify_channel`: Mock _do_ws_relay to return (None, "error"), verify the error notify_all call also passes channel.
   - `test_cron_job_no_notify_channel_broadcasts`: Job without notify_channel field, verify notify_all called with `channel=None`.

Follow existing test patterns in the file (unittest.TestCase, mock.patch decorators).
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python3 -m pytest test_gateway.py -x -k "notify_channel or NotifyChannel or CronNotifyChannel" -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - handle_notify extracts optional channel from request body and passes to notify_all
    - _fire_cron_job passes job's notify_channel to both notify_all calls (error and success)
    - All new tests pass
    - Existing tests still pass (no regression)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --notify-channel flag to remind.sh</name>
  <files>docker/scripts/remind.sh</files>
  <action>
**CHAN-09: remind.sh --notify-channel flag**

In `cmd_create()`, add a `notify_channel` local variable and parse the flag. Follow the exact pattern from job.sh (lines 240-242):

1. Add `local notify_channel=""` alongside the other local variable declarations at the top of cmd_create.

2. Add a case in the while loop:
```bash
--notify-channel)
    shift
    notify_channel="$1"
    ;;
```

3. In the PAYLOAD python block, add the `_NOTIFY_CH` env var and include it in the JSON:
   - Add `_NOTIFY_CH="$notify_channel"` to the env vars before the python3 -c call
   - In the python code, add: `nc = os.environ.get('_NOTIFY_CH', '')` and `if nc: d['notify_channel'] = nc`

4. Update the usage text at the bottom of the file to include `--notify-channel`:
```
echo "  remind \"message\" --in 5m [--notify-channel telegram]"
```

Reference job.sh lines 212, 240-242, 284, 298, 314 for the exact pattern.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && bash -n docker/scripts/remind.sh && echo "syntax OK" && grep -c "notify-channel" docker/scripts/remind.sh</automated>
    <manual>Verify remind.sh --notify-channel flag is parsed and included in the API payload by reading the cmd_create function</manual>
  </verify>
  <done>
    - remind.sh accepts --notify-channel flag in cmd_create
    - Flag value is passed as notify_channel in the JSON payload to POST /api/jobs
    - Usage text updated to show the flag
    - Shell syntax is valid (bash -n passes)
  </done>
</task>

</tasks>

<verification>
- All existing tests pass: `cd /Users/haseeb/nix-template/docker && python3 -m pytest test_gateway.py -x -v 2>&1 | tail -5`
- New channel targeting tests pass: `python3 -m pytest test_gateway.py -x -k "NotifyChannel or CronNotifyChannel" -v`
- remind.sh syntax valid: `bash -n docker/scripts/remind.sh`
- grep confirms wiring: `grep -n "notify_all.*channel=" docker/gateway.py | head -10`
</verification>

<success_criteria>
1. POST /api/notify with `{"text": "hello", "channel": "telegram"}` calls notify_all with channel="telegram" (targeted delivery)
2. POST /api/notify with `{"text": "hello"}` (no channel) calls notify_all with channel=None (broadcast, backward compatible)
3. _fire_cron_job passes job's notify_channel to notify_all for both success and error paths
4. remind.sh accepts --notify-channel flag and includes notify_channel in the job creation payload
5. All existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/08-notification-channel-targeting/08-01-SUMMARY.md`
</output>
