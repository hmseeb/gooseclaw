---
phase: 10-multi-bot-lifecycle
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - docker/gateway.py
  - docker/test_gateway.py
autonomous: true
requirements: [BOT-05, BOT-06]

must_haves:
  truths:
    - "POST /api/bots with name+token creates a running bot that begins polling immediately"
    - "DELETE /api/bots/<name> stops the bot's poll loop and cleans up sessions"
    - "Adding a bot persists to setup.json bots array so it survives restart"
    - "Removing a bot removes from setup.json bots array so it stays gone after restart"
    - "Adding or removing a bot does not interrupt other running bots"
  artifacts:
    - path: "docker/gateway.py"
      provides: "handle_add_bot, handle_remove_bot endpoints, unregister_notification_handler, enhanced remove_bot"
      contains: "def handle_add_bot"
    - path: "docker/test_gateway.py"
      provides: "Tests for hot-add, hot-remove, persistence, non-interference"
      contains: "class TestBotLifecycleAPI"
  key_links:
    - from: "do_POST"
      to: "handle_add_bot"
      via: "path == /api/bots"
      pattern: 'path == "/api/bots"'
    - from: "do_DELETE"
      to: "handle_remove_bot"
      via: "path.startswith(/api/bots/)"
      pattern: 'path.startswith("/api/bots/")'
    - from: "handle_add_bot"
      to: "save_setup"
      via: "persist new bot to setup.json"
      pattern: "save_setup"
    - from: "handle_remove_bot"
      to: "save_setup"
      via: "persist bot removal to setup.json"
      pattern: "save_setup"
---

<objective>
Add hot-add and hot-remove API endpoints for Telegram bots with setup.json persistence.

Purpose: Operators can dynamically manage bots without container restart, completing the multi-bot lifecycle.
Output: POST /api/bots and DELETE /api/bots/<name> endpoints, with full session cleanup and config persistence.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-multi-bot-lifecycle/10-RESEARCH.md
@docker/gateway.py (BotManager ~line 592, BotInstance ~line 262, apply_config ~line 2167, do_POST ~line 5472, do_DELETE ~line 5522, handle_telegram_status ~line 6201, _notification_handlers ~line 687, SessionManager.clear_channel ~line 175)
@docker/test_gateway.py (TestBotManager ~line 2249, TestBotAPIEndpoints ~line 2807)
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED -- Failing tests for bot lifecycle API</name>
  <files>docker/test_gateway.py</files>
  <action>
Add a new test class `TestBotLifecycleAPI` at the end of the bot test section (after `TestBotShutdown`). Follow the same mock handler pattern used in `TestBotAPIEndpoints._make_handler()`.

Tests to write (all should FAIL initially):

**unregister_notification_handler:**
1. `test_unregister_notification_handler` -- register a handler, unregister it by name, verify it's gone from `_notification_handlers`
2. `test_unregister_nonexistent_handler` -- unregister a handler that doesn't exist, should not raise

**BotManager.remove_bot enhanced cleanup:**
3. `test_remove_bot_calls_stop` -- add a bot, set bot.running = True, call remove_bot, verify bot.running is False (already passes, but also verify bot.stop() would be called by checking thread join behavior)
4. `test_remove_bot_clears_sessions` -- add a bot with channel_key "telegram:test", set a session via _session_manager.set("telegram:test", "user1", "sid1"), call remove_bot, verify _session_manager.get("telegram:test", "user1") returns None
5. `test_remove_bot_unregisters_notification` -- add a bot, register a notification handler with the bot's channel_key, call remove_bot, verify handler is gone

**POST /api/bots (hot-add):**
6. `test_add_bot_success` -- POST with {name: "research", token: "111:AAA"}, mock save_setup and bot.start, verify 201 response with bot name and status
7. `test_add_bot_missing_name` -- POST with {token: "111:AAA"} (no name), verify 400
8. `test_add_bot_missing_token` -- POST with {name: "research"} (no token), verify 400
9. `test_add_bot_invalid_token_format` -- POST with {name: "research", token: "badtoken"} (no colon), verify 400
10. `test_add_bot_duplicate_name` -- add a bot "research" to _bot_manager first, then POST with same name, verify 409
11. `test_add_bot_persists_to_setup` -- POST with valid bot, verify save_setup was called with config containing the new bot in bots array

**DELETE /api/bots/<name> (hot-remove):**
12. `test_remove_bot_success` -- add bot "research" to _bot_manager, DELETE /api/bots/research, mock save_setup, verify 200 and bot is gone from _bot_manager
13. `test_remove_bot_not_found` -- DELETE /api/bots/nonexistent, verify 404
14. `test_remove_bot_persists_to_setup` -- add bot, DELETE it, verify save_setup was called with config that no longer contains the removed bot
15. `test_remove_bot_does_not_affect_others` -- add bots "a" and "b", remove "a", verify "b" still in _bot_manager and its running state unchanged

**Auth:**
16. `test_add_bot_requires_auth` -- POST from non-localhost without auth, verify 401
17. `test_remove_bot_requires_auth` -- DELETE from non-localhost without auth, verify 401

Use setUp/tearDown to clear _bot_manager._bots and restore _notification_handlers. Mock `save_setup`, `load_setup`, `bot.start()` (to avoid actual Telegram API calls). Mock `_is_first_boot` to return False.

For the handler mock, use the same pattern as TestBotAPIEndpoints:
- Create mock handler with `_make_handler(path, method)`
- Call `gateway.GatewayHandler.handle_add_bot(handler)` or `gateway.GatewayHandler.handle_remove_bot(handler, "name")`
- Check `handler._json_response` tuple (status, data)

For POST body, mock `_read_body` to return JSON string.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python -m pytest docker/test_gateway.py::TestBotLifecycleAPI -x 2>&1 | tail -5</automated>
    <manual>All new tests should FAIL (methods don't exist yet)</manual>
  </verify>
  <done>17 new test methods in TestBotLifecycleAPI, all failing because handle_add_bot, handle_remove_bot, and unregister_notification_handler don't exist yet</done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- Implement bot lifecycle API endpoints</name>
  <files>docker/gateway.py</files>
  <action>
Implement the following in gateway.py to make all tests pass:

**1. `unregister_notification_handler(name)` (near line 700, after register_notification_handler):**
```
def unregister_notification_handler(name):
    with _notification_handlers_lock:
        _notification_handlers[:] = [h for h in _notification_handlers if h["name"] != name]
    print(f"[notify] unregistered handler: {name}")
```

**2. Enhance `BotManager.remove_bot()` (line 610):**
Replace existing body with:
- Pop bot from registry (under lock, same as now)
- If bot found: call `bot.stop()` (joins thread), call `_session_manager.clear_channel(bot.channel_key)`, call `unregister_notification_handler(bot.channel_key)`
- Log removal

**3. `handle_add_bot()` method on GatewayHandler (near handle_telegram_pair):**
- Guard: `_is_first_boot()` -> 403
- Guard: `_check_local_or_auth()` -> 401 (same pattern as handle_create_job)
- Read body, parse JSON
- Validate: `name` required (string, non-empty), `token` required (string, must contain `:`), else 400
- Check `_bot_manager.get_bot(name)` -- if exists, return 409 `{"error": "bot already exists"}`
- Create bot via `_bot_manager.add_bot(name, token, channel_key)` where channel_key = "telegram" if name == "default" else f"telegram:{name}"
- Call `bot.start()`
- Persist: `load_setup()`, ensure `bots` key is a list, append `{"name": name, "token": token}` (include provider/model if provided in body), call `save_setup(config)`
- If provider/model provided in body, include them in the bot config dict
- Return 201 with `{"name": name, "channel_key": bot.channel_key, "status": "running"}`
- Wrap in try/except: ValueError from add_bot (duplicate token) -> 409, other exceptions -> 500

**4. `handle_remove_bot(name)` method on GatewayHandler:**
- Guard: `_is_first_boot()` -> 403
- Guard: `_check_local_or_auth()` -> 401
- Check `_bot_manager.get_bot(name)` -- if None, return 404 `{"error": "bot not found"}`
- If name == "default", log warning: `[bot-mgr] warn: removing default bot`
- Call `_bot_manager.remove_bot(name)` (enhanced version handles stop + cleanup)
- Persist: `load_setup()`, filter bot out of `bots` array, call `save_setup(config)`. If config had no `bots` array (legacy single-token), set `telegram_bot_token` to empty string.
- Return 200 with `{"removed": name}`

**5. Route wiring in `do_POST` (line 5472 area):**
Add before the final `else: self.proxy_to_goose()`:
```python
elif path == "/api/bots":
    self.handle_add_bot()
```

**6. Route wiring in `do_DELETE` (line 5522 area):**
Add before the final `self.proxy_to_goose()`:
```python
elif path.startswith("/api/bots/"):
    bot_name = path[len("/api/bots/"):]
    if bot_name:
        self.handle_remove_bot(bot_name)
        return
```

Do NOT add rate limiting beyond what already exists (all /api/* GET paths are already rate-limited; POST/DELETE use _check_local_or_auth which is sufficient).
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python -m pytest docker/test_gateway.py -x 2>&1 | tail -5</automated>
    <manual>All 265+ tests pass (248 existing + 17 new)</manual>
  </verify>
  <done>POST /api/bots creates and starts a bot with setup.json persistence. DELETE /api/bots/<name> stops bot, clears sessions, unregisters notifications, removes from setup.json. All tests pass with zero regressions.</done>
</task>

</tasks>

<verification>
1. `python -m pytest docker/test_gateway.py -x` -- all tests pass
2. `python -m pytest docker/test_gateway.py::TestBotLifecycleAPI -v` -- all 17 lifecycle tests pass
3. `grep -c "def test_" docker/test_gateway.py` -- total test count increased by 17
</verification>

<success_criteria>
- POST /api/bots with {name, token} returns 201 and bot starts polling
- DELETE /api/bots/<name> returns 200, bot stops, sessions cleared, notification handler removed
- Both endpoints persist changes to setup.json bots array
- Both endpoints require auth from non-localhost
- Removing a bot does not affect other running bots
- All existing 248 tests continue to pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/10-multi-bot-lifecycle/10-01-SUMMARY.md`
</output>
