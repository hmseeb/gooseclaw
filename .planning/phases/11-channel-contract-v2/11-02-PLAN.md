---
phase: 11-channel-contract-v2
plan: 02
type: tdd
wave: 2
depends_on: [11-01]
files_modified:
  - docker/gateway.py
  - docker/test_gateway.py
autonomous: true
requirements:
  - MEDIA-05

must_haves:
  truths:
    - "_load_channel wraps legacy send(text) plugins in LegacyOutboundAdapter automatically"
    - "_load_channel detects v2 adapter field and uses it directly"
    - "ChannelRelay.__call__ accepts InboundMessage as first argument"
    - "ChannelRelay.__call__ still accepts legacy (user_id, text, send_fn) signature"
    - "BotInstance._poll_loop creates InboundMessage envelopes from Telegram update JSON"
    - "GET /api/channels includes capabilities in response"
  artifacts:
    - path: "docker/gateway.py"
      provides: "Updated _load_channel, ChannelRelay, BotInstance with v2 contract"
      contains: "LegacyOutboundAdapter"
    - path: "docker/test_gateway.py"
      provides: "Integration tests for wiring"
      contains: "class TestLoadChannelV2"
---

<objective>
Wire the four new types into the existing plugin system using TDD.

Purpose: _load_channel wraps legacy plugins in LegacyOutboundAdapter, ChannelRelay accepts InboundMessage, BotInstance creates InboundMessage envelopes, and GET /api/channels exposes capabilities. All backward-compatible.

Output: Existing plugin system works unchanged but now uses the v2 contract internally.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-channel-contract-v2/11-RESEARCH.md
@docker/gateway.py (lines 3524-3639 for _load_channel, lines 3396-3515 for ChannelRelay, lines 454-599 for BotInstance._poll_loop, lines 3577-3586 for notification handler registration)
@docker/test_gateway.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED -- Write failing tests for wiring</name>
  <files>docker/test_gateway.py</files>
  <action>
Add test classes to docker/test_gateway.py BEFORE the `if __name__` block.

**TestLoadChannelV2 class** (covers MEDIA-05):
- test_legacy_plugin_wrapped: Create a mock plugin module with CHANNEL={"name":"test","version":1,"send":mock_fn}. After _load_channel processes it, the stored adapter should be a LegacyOutboundAdapter. adapter.send_text("hi") calls mock_fn.
- test_v2_plugin_used_directly: Create a mock plugin with CHANNEL={"name":"test","version":2,"adapter":custom_adapter}. The stored adapter should be the custom_adapter instance, not wrapped.
- test_legacy_notification_uses_adapter: After loading a legacy plugin, the notification handler should delegate to adapter.send_text (functionally equivalent to calling send_fn directly).

**TestChannelRelayV2 class**:
- test_relay_accepts_inbound_message: ChannelRelay.__call__ with InboundMessage(user_id="123", text="hello") processes without error. (Mock _relay_to_goose_web to avoid real WS connection.)
- test_relay_still_accepts_legacy_args: ChannelRelay.__call__(user_id="123", text="hello") still works (backward compat).
- test_relay_inbound_message_extracts_text: When called with InboundMessage, the text field is forwarded to the relay.

**TestBotInboundMessage class**:
- test_text_message_creates_inbound: Given a Telegram update with text="hello" and chat_id="123", BotInstance._poll_loop should produce an InboundMessage with user_id="123", text="hello", channel containing "telegram".
- test_media_message_creates_inbound_with_media: Given a Telegram update with photo=[...] and no text, should produce InboundMessage with has_media=True, media list populated with type="image".
- test_text_with_caption_creates_inbound: Given update with photo + caption, InboundMessage should have both text and media.

**TestChannelsAPICapabilities class**:
- test_api_channels_includes_capabilities: GET /api/channels response should include "capabilities" key for each loaded channel.

These tests will require mocking gateway internals. Use unittest.mock extensively.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -m pytest docker/test_gateway.py -k "TestLoadChannelV2 or TestChannelRelayV2 or TestBotInboundMessage or TestChannelsAPICapabilities" --tb=short 2>&1 | tail -15</automated>
    <manual>New tests FAIL (classes/methods don't have v2 behavior yet). All existing tests still pass.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All new test methods exist and fail. Existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- Wire v2 types into plugin system</name>
  <files>docker/gateway.py</files>
  <action>
Wire the four new types into the existing plugin system:

1. **_load_channel() update** (around line 3524):
   - After extracting send_fn from CHANNEL dict, check for CHANNEL.get("adapter")
   - If adapter exists and is OutboundAdapter instance: use it
   - If no adapter but send_fn exists: wrap in LegacyOutboundAdapter(send_fn)
   - Store adapter on the loaded channel metadata in _loaded_channels
   - Register adapter.send_text (wrapped in _make_handler) with notification bus instead of raw send_fn

2. **ChannelRelay.__call__ update** (around line 3407):
   - Accept first argument as either InboundMessage or user_id string
   - If InboundMessage: extract user_id, text from it
   - If string: wrap in InboundMessage(user_id=first_arg, text=second_arg) for backward compat
   - Rest of relay logic uses msg.user_id and msg.text (functionally identical for text-only)

3. **BotInstance._poll_loop update** (around line 484):
   - After extracting chat_id and text from Telegram update JSON
   - Create InboundMessage(user_id=chat_id, text=text, channel=self.channel_key)
   - For media messages: populate media list with dicts from _MEDIA_KEYS detection
   - Still call _do_message_relay with text only (media relay is Phase 12-13)
   - Media messages still get MEDIA_REPLY for now (actual processing in Phase 12)

4. **GET /api/channels update** (around line 6757):
   - Include capabilities in channel info dict: channel_info["capabilities"] = adapter.capabilities().to_dict()

Do NOT change notify_all signature. Do NOT change _relay_to_goose_web. These are Phase 12+ concerns.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -m pytest docker/test_gateway.py -v --tb=short 2>&1 | tail -30</automated>
    <manual>All new and existing tests pass. Full suite green.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All tests pass. v2 contract wired into plugin system. Backward compat verified.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest docker/test_gateway.py -v 2>&1 | tail -5` -- all tests pass
2. Verify legacy plugin path: existing tests that use the plugin system still pass unchanged
3. Verify API: `python3 -c "import sys; sys.path.insert(0,'docker'); import gateway; print(gateway.ChannelCapabilities(supports_images=True).to_dict())"` -- shows capabilities dict
</verification>

<success_criteria>
- _load_channel wraps v1 plugins in LegacyOutboundAdapter automatically
- _load_channel uses v2 adapter directly when present
- ChannelRelay accepts both InboundMessage and legacy (user_id, text) signatures
- BotInstance._poll_loop creates InboundMessage envelopes
- GET /api/channels includes capabilities
- All existing 312+ tests still pass (zero regression)
- Notification bus works unchanged through adapter.send_text
</success_criteria>

<output>
After completion, create `.planning/phases/11-channel-contract-v2/11-02-SUMMARY.md`
</output>
