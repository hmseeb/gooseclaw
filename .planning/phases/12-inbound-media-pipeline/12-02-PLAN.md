---
phase: 12-inbound-media-pipeline
plan: 02
type: tdd
wave: 2
depends_on: [12-01]
files_modified:
  - docker/gateway.py
  - docker/test_gateway.py
autonomous: true
requirements:
  - MEDIA-06
  - MEDIA-08
  - MEDIA-09

must_haves:
  truths:
    - "BotInstance._poll_loop passes file_id references in InboundMessage.media for downloading"
    - "BotInstance._do_message_relay downloads media and creates MediaContent objects before relay"
    - "MEDIA_REPLY early-return removed for paired users with media messages"
    - "Legacy _telegram_poll_loop also handles media download flow"
    - "Media-only messages (no text) flow through to the relay instead of being rejected"
    - "Download failures are handled gracefully (logged, media skipped, relay continues with text if any)"
  artifacts:
    - path: "docker/gateway.py"
      provides: "Updated BotInstance._poll_loop, _do_message_relay, _telegram_poll_loop with media download"
    - path: "docker/test_gateway.py"
      provides: "Integration tests for media download in poll/relay path"
      contains: "class TestBotMediaDownload"
---

<objective>
Wire media download into BotInstance poll/relay path and legacy _telegram_poll_loop using TDD.

Purpose: Replace the MEDIA_REPLY rejection with actual media download and normalization. Media messages from paired users now flow through the relay with InboundMessage.media populated with MediaContent objects (or file_id references for deferred download).

Output: Media messages are downloaded, normalized, and available in InboundMessage.media for Phase 13's relay upgrade.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-inbound-media-pipeline/12-RESEARCH.md
@docker/gateway.py (lines 454-599 for BotInstance._poll_loop, lines 356-452 for _do_message_relay, lines 5200-5300 for _telegram_poll_loop, lines 1134-1144 for MEDIA_REPLY/_has_media)
@docker/test_gateway.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED -- Write failing tests for media wiring</name>
  <files>docker/test_gateway.py</files>
  <action>
Add test classes to docker/test_gateway.py BEFORE the `if __name__` block.

**TestBotMediaDownload class**:
- test_photo_message_downloads_and_creates_media_content: Mock a Telegram update with photo, mock _download_telegram_file to return (b"jpeg_bytes", "photos/file_0.jpg"). Verify _do_message_relay receives InboundMessage with media containing a MediaContent with kind="image".
- test_voice_message_downloads: Mock update with voice, mock download to return (b"ogg_bytes", "voice/file.oga"). Verify MediaContent has kind="audio", mime_type contains "ogg" or "audio".
- test_document_message_downloads: Mock update with document (file_name="report.pdf", mime_type="application/pdf"). Verify MediaContent has kind="document", filename="report.pdf".
- test_media_only_no_longer_rejected: A media-only message (photo, no text) from a paired user should NOT receive MEDIA_REPLY. It should flow through to _do_message_relay. (Mock send_telegram_message, verify MEDIA_REPLY is NOT sent.)
- test_download_failure_graceful: Mock _download_telegram_file returning (None, "error"). Verify relay continues without crashing. InboundMessage.media should be empty or contain None.
- test_text_with_caption_preserves_both: Message with photo + caption text. InboundMessage should have both text=caption and media with image.
- test_unpaired_media_still_silent: Media from unpaired user should be silently ignored (no MEDIA_REPLY, no relay).

**TestLegacyPollMediaDownload class**:
- test_legacy_photo_downloads: Same as BotInstance test but for _telegram_poll_loop path. Photo from paired user triggers download.
- test_legacy_media_only_flows: Media-only message in legacy path also flows through to relay (no MEDIA_REPLY).

Use extensive mocking. Mock _download_telegram_file, send_telegram_message, _relay_to_goose_web, get_paired_chat_ids.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -m pytest docker/test_gateway.py -k "TestBotMediaDownload or TestLegacyPollMediaDownload" --tb=short 2>&1 | tail -15</automated>
    <manual>New tests FAIL. All existing tests still pass.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All new test methods exist and fail. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- Wire media download into poll/relay paths</name>
  <files>docker/gateway.py</files>
  <action>
**1. Update BotInstance._poll_loop** (around line 490):

Replace the MEDIA_REPLY early-return block. Instead of rejecting media:
- For paired users with media: extract file_id references using _extract_file_info for each detected media key
- Store as dicts in InboundMessage.media: [{"media_key": "photo", "file_id": "xxx", "mime_hint": None, "filename": None}]
- Pass InboundMessage to _do_message_relay as before (it already accepts inbound_msg kwarg)
- For unpaired users with media: continue silently (no reply, no relay)
- Remove the MEDIA_REPLY send for paired users

**2. Update BotInstance._do_message_relay** (around line 356):

At the start of _do_message_relay, after acquiring the user lock:
- Check if inbound_msg has media references (list of dicts with file_id)
- For each media reference, call _download_telegram_file(bot_token, ref["file_id"])
- If download succeeds: create MediaContent via _make_media_content
- If download fails: log warning, skip this attachment
- Replace inbound_msg.media with list of MediaContent objects
- Relay continues with msg.text (actual multimodal relay is Phase 13)

**3. Update legacy _telegram_poll_loop** (around line 5200):

Mirror the BotInstance changes:
- Remove MEDIA_REPLY for paired users
- Extract file_id references for media messages
- In the relay thread, download before relaying
- Unpaired users with media: silently skip

**4. Keep MEDIA_REPLY constant** (don't delete it, may be used by tests or future features).

Key: Downloads happen in the relay thread (already background-threaded), NOT in the poll loop.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -m pytest docker/test_gateway.py -v --tb=short 2>&1 | tail -30</automated>
    <manual>All new and existing tests pass. Full suite green.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All tests pass. Media messages download and normalize. MEDIA_REPLY no longer sent to paired users.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest docker/test_gateway.py -v 2>&1 | tail -5` -- all tests pass
2. Verify MEDIA_REPLY is no longer sent to paired users (grep for send_telegram_message.*MEDIA_REPLY in poll loops)
3. Verify _download_telegram_file is called in relay thread, not poll loop
</verification>

<success_criteria>
- Media messages from paired users are downloaded via _download_telegram_file
- MediaContent objects created with correct kind/mime/data
- MEDIA_REPLY no longer sent to paired users (they get actual processing)
- Download failures handled gracefully (logged, skipped)
- Media-only messages flow through to relay (not rejected)
- Legacy _telegram_poll_loop also updated
- All new + existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-inbound-media-pipeline/12-02-SUMMARY.md`
</output>
