---
phase: 13-relay-protocol-upgrade
plan: 02
type: tdd
wave: 2
depends_on: [13-01]
files_modified:
  - docker/gateway.py
  - docker/test_gateway.py
autonomous: true
requirements:
  - MEDIA-10
  - MEDIA-11
  - MEDIA-12

must_haves:
  truths:
    - "_relay_to_goose_web calls _do_rest_relay instead of _do_ws_relay"
    - "_relay_to_goose_web accepts content_blocks kwarg and passes to rest relay"
    - "_relay_to_goose_web returns 3-tuple (text, error, media_blocks)"
    - "All 12 call sites updated to unpack 3-tuple without ValueError"
    - "BotInstance._do_message_relay passes content_blocks from InboundMessage media to relay"
    - "ChannelRelay.__call__ passes content blocks from InboundMessage through to relay"
    - "Legacy _telegram_poll_loop passes content_blocks to relay"
    - "WS relay functions (_do_ws_relay, _do_ws_relay_streaming) are removed or commented out"
    - "WS helper functions (_ws_connect, _ws_send_text, _ws_recv_frame, _ws_recv_text) are removed or commented out"
    - "Text-only messages still work identically (backward compatible)"
  artifacts:
    - path: "docker/gateway.py"
      provides: "Updated _relay_to_goose_web, all callers, removed WS code"
    - path: "docker/test_gateway.py"
      provides: "Integration tests for relay wiring"
      contains: "class TestRelayProtocolUpgrade"
---

<objective>
Wire REST relay into _relay_to_goose_web, update all 12 call sites, remove WS code using TDD.

Purpose: Replace the WebSocket relay with REST /reply + SSE. Every caller of _relay_to_goose_web now uses the REST path. Content blocks from InboundMessage media flow through to goose. Responses carry media_blocks for Phase 14's outbound routing.

Output: The relay protocol is fully upgraded. Text-only messages work identically. Multimodal messages send content blocks to goose.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-relay-protocol-upgrade/13-RESEARCH.md (call sites table, pattern 4)
@docker/gateway.py (lines 4848-4892 for _relay_to_goose_web, lines 395-465 for BotInstance._do_message_relay, lines 595-620 for kick greeting, lines 3680-3720 for ChannelRelay, lines 4285-4300 for compact, lines 5380-5590 for legacy poll, lines 4663-5251 for WS code to remove)
@docker/test_gateway.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED -- Write failing tests for relay wiring</name>
  <files>docker/test_gateway.py</files>
  <action>
Add test class to docker/test_gateway.py BEFORE the `if __name__` block.

**TestRelayProtocolUpgrade class**:
- test_relay_returns_3_tuple: Mock _do_rest_relay to return ("hi","",[]). Call _relay_to_goose_web("test", "sid"). Verify returns 3 values (text, err, media).
- test_relay_passes_content_blocks: Mock _do_rest_relay. Call _relay_to_goose_web("test", "sid", content_blocks=[{"type":"text","text":"test"}]). Verify _do_rest_relay received content_blocks.
- test_relay_uses_rest_not_ws: Mock _do_rest_relay and _do_ws_relay. Call _relay_to_goose_web. Verify _do_rest_relay was called, _do_ws_relay was NOT called.
- test_relay_streaming_uses_rest: Mock _do_rest_relay_streaming. Call _relay_to_goose_web with flush_cb and verbosity="balanced". Verify _do_rest_relay_streaming was called.
- test_relay_retry_on_error_returns_3_tuple: Mock _do_rest_relay to fail first then succeed. With chat_id set, verify retry returns 3-tuple.
- test_bot_relay_builds_content_blocks_from_media: Mock _relay_to_goose_web. Create BotInstance, simulate _do_message_relay with InboundMessage containing image MediaContent. Verify _relay_to_goose_web received content_blocks with image block.
- test_bot_relay_text_only_no_content_blocks: Mock _relay_to_goose_web. Simulate relay with text-only InboundMessage. Verify content_blocks is None (text-only path unchanged).
- test_all_callers_unpack_3_tuple: This is a meta-test -- grep gateway.py for `= _relay_to_goose_web(` and verify none use 2-value unpack. (Use ast or regex in test.)

These tests will fail because _relay_to_goose_web still returns 2-tuple and uses WS.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -m pytest docker/test_gateway.py -k "TestRelayProtocolUpgrade" --tb=short 2>&1 | tail -15</automated>
    <manual>New tests FAIL. All existing tests still pass.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All new test methods exist and fail. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- Wire REST relay and update all callers</name>
  <files>docker/gateway.py</files>
  <action>
**1. Update _relay_to_goose_web** (line ~4848):
- Add `content_blocks=None` parameter
- Change docstring: returns (response_text, error_string, media_blocks)
- Replace relay_fn lambdas:
  - streaming: `lambda txt, sid: _do_rest_relay_streaming(txt, sid, flush_cb, verbosity, content_blocks=content_blocks, sock_ref=sock_ref, flush_interval=flush_interval)`
  - non-streaming: `lambda txt, sid: _do_rest_relay(txt, sid, content_blocks=content_blocks, sock_ref=sock_ref)`
- Update `text, err = relay_fn(...)` to `text, err, media = relay_fn(...)`
- On retry: `return relay_fn(user_text, new_sid)` (already returns 3-tuple)
- Final return: `return text, err, media` (was `return text, err`)
- Handle the early return for no token: `return "", "Goose is not ready...", []`

**2. Update all 12 call sites** (from research table):
- Line ~410 (BotInstance quiet): `response_text, error, media = _relay_to_goose_web(...)`
- Line ~445 (BotInstance streaming): `response_text, error, media = _relay_to_goose_web(...)`
- Line ~610 (kick greeting): `txt, err, *_ = _relay_to_goose_web(...)`
- Line ~3689 (ChannelRelay streaming): `response_text, error, *_ = _relay_to_goose_web(...)`
- Line ~3695 (ChannelRelay non-streaming): `response_text, error, *_ = _relay_to_goose_web(...)`
- Line ~3708 (ChannelRelay retry streaming): `response_text, error, *_ = _relay_to_goose_web(...)`
- Line ~3714 (ChannelRelay retry non-streaming): `response_text, error, *_ = _relay_to_goose_web(...)`
- Line ~4295 (compact): `response_text, error, *_ = _relay_to_goose_web(...)`
- Line ~5390 (legacy media relay): `response_text, error, *_ = _relay_to_goose_web(...)`
- Line ~5483 (legacy quiet): `response_text, error, *_ = _relay_to_goose_web(...)`
- Line ~5518 (legacy streaming): `response_text, error, *_ = _relay_to_goose_web(...)`
- Line ~5584 (legacy kick greeting): `txt, err, *_ = _relay_to_goose_web(...)`

**3. Pass content_blocks in BotInstance._do_message_relay**:
After media download creates MediaContent objects in inbound_msg.media, build content blocks:
```python
content_blocks = None
if inbound_msg and inbound_msg.has_media:
    content_blocks = _build_content_blocks(text, inbound_msg)
```
Then pass `content_blocks=content_blocks` to _relay_to_goose_web calls (both quiet and streaming).

**4. Pass content_blocks in legacy _telegram_poll_loop**:
Same pattern as BotInstance -- build content_blocks from InboundMessage media before relay.

**5. Remove WS code**:
Delete (not comment out) the following functions:
- _ws_connect (~line 4665)
- _ws_send_text (~line 4711)
- _ws_recv_frame (~line 4740)
- _ws_recv_text (~line 4771)
- _do_ws_relay (~line 4933)
- _do_ws_relay_streaming (~line 5100)
Remove the "minimal WebSocket client" section header comment.
Keep struct import if used elsewhere (it is, for setup.json binary).

**6. Keep _StreamBuffer, _truncate, _diagnose_empty_response** untouched.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -m pytest docker/test_gateway.py -v --tb=short 2>&1 | tail -30</automated>
    <manual>All new and existing tests pass. Full suite green.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All tests pass. Relay uses REST. All callers updated. WS code removed.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest docker/test_gateway.py -v 2>&1 | tail -5` -- all tests pass
2. `grep -n "_do_ws_relay\|_ws_connect\|_ws_send_text\|_ws_recv" docker/gateway.py | wc -l` -- should be 0
3. `grep -c "response_text, error = _relay_to_goose_web\|txt, err = _relay_to_goose_web" docker/gateway.py` -- should be 0
4. `grep -c "content_blocks" docker/gateway.py` -- should be > 5
</verification>

<success_criteria>
- _relay_to_goose_web uses REST relay, returns 3-tuple
- All 12 call sites unpack 3-tuple correctly
- BotInstance._do_message_relay sends content_blocks with media
- WS relay code completely removed
- Text-only messages still work identically
- All new + existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-relay-protocol-upgrade/13-02-SUMMARY.md`
</output>
