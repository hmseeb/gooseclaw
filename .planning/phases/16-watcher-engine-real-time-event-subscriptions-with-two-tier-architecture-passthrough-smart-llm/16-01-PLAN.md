---
phase: 16-watcher-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - docker/gateway.py
  - docker/test_gateway.py
autonomous: true
requirements:
  - WATCH-01
  - WATCH-02
  - WATCH-03
  - WATCH-04

must_haves:
  truths:
    - "create_watcher() validates type (webhook/feed/stream), rejects invalid, returns (watcher, error)"
    - "create_watcher() rejects duplicate IDs"
    - "delete_watcher() removes by ID, returns True/False"
    - "list_watchers() returns all watchers"
    - "_process_passthrough() renders string.Template with safe_substitute on flattened payload"
    - "_process_passthrough() returns JSON dump when no template is set"
    - "Watchers persist to watchers.json and reload on _load_watchers()"
  artifacts:
    - path: "docker/gateway.py"
      provides: "Watcher CRUD functions, passthrough processing, persistence"
      contains: "def create_watcher"
    - path: "docker/test_gateway.py"
      provides: "Tests for CRUD + passthrough"
      contains: "class TestCreateWatcher"
  key_links:
    - from: "docker/gateway.py"
      to: "data/watchers.json"
      via: "_save_watchers/_load_watchers"
      pattern: "_save_watchers|_load_watchers"
---

<objective>
TDD: Build watcher CRUD (create, list, delete, update), JSON persistence, and tier-1 passthrough template processing.

Purpose: Foundation layer. All other watcher plans depend on being able to create, persist, and process watchers.
Output: Working CRUD functions + passthrough processing with full test coverage.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-watcher-engine-real-time-event-subscriptions-with-two-tier-architecture-passthrough-smart-llm/16-RESEARCH.md
@docker/gateway.py
@docker/test_gateway.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD watcher CRUD + persistence</name>
  <files>docker/gateway.py, docker/test_gateway.py</files>
  <action>
RED: Write test classes in test_gateway.py following existing TestCreateJob pattern:
- TestCreateWatcher: test valid webhook/feed/stream types, test invalid type rejected, test duplicate ID rejected, test auto-generated ID, test feed requires source URL, test default values populated (enabled=True, fire_count=0, etc.)
- TestDeleteWatcher: test delete existing returns True, test delete nonexistent returns False, test delete removes from list
- TestListWatchers: test returns all watchers, test empty list
- TestUpdateWatcher: test update fields, test update nonexistent returns error

Mock _save_watchers (same pattern as existing _save_jobs mocks in test file). Use @patch decorators.

GREEN: Implement in gateway.py (place after job engine section, before HTTP handler):
- Module-level state: _WATCHERS_FILE, _watchers list, _watchers_lock
- _load_watchers(): read watchers.json, populate _watchers list
- _save_watchers(): atomic write via .tmp + os.replace (same as _save_jobs pattern)
- create_watcher(data): validate type in (webhook/feed/stream), check duplicate ID, build watcher dict with all fields from research Pattern 1, append and save
- delete_watcher(id): find and remove by ID, save, return bool
- list_watchers(): return copy of _watchers list
- update_watcher(id, updates): find by ID, merge updates (only allowed fields: name, enabled, transform, prompt, channel, filter, poll_seconds, headers, webhook_secret), save

Run tests, ensure all pass.

REFACTOR: Extract _make_watcher_defaults() if create_watcher gets long.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python -m pytest test_gateway.py -k "TestCreateWatcher or TestDeleteWatcher or TestListWatcher or TestUpdateWatcher" -x -q --tb=short</automated>
  </verify>
  <done>create_watcher validates and returns watcher dicts, delete/list/update work correctly, persistence mocked and tested</done>
</task>

<task type="auto">
  <name>Task 2: TDD passthrough template processing</name>
  <files>docker/gateway.py, docker/test_gateway.py</files>
  <action>
RED: Write TestPassthroughProcess class:
- test_empty_template_returns_json_dump: no transform -> JSON dump of data truncated to 2000 chars
- test_simple_template: transform="${repo}: ${action}" with {"repo":"foo","action":"push"} -> "foo: push"
- test_nested_payload_flattened: {"repository":{"name":"foo"}} accessible as ${repository_name}
- test_missing_key_safe_substitute: template with ${missing} leaves literal ${missing} (safe_substitute)
- test_double_brace_syntax: transform="{{repo}}: {{action}}" converted to ${repo}: ${action} before processing
- test_truncation: large payload JSON dump truncated to 2000 chars

GREEN: Implement in gateway.py:
- _flatten_dict(d, prefix="", sep="_"): recursive flattening, keep both leaf keys and full paths (from research Pattern 3)
- _convert_double_braces(tmpl): simple regex re.sub(r'\{\{(\w+)\}\}', r'${\1}', tmpl) to support {{var}} syntax
- _process_passthrough(watcher, data): if no transform, return json.dumps(data, indent=2)[:2000]. Otherwise, flatten data, convert double braces in template, use string.Template.safe_substitute.

Import string at top of gateway.py (or use from string import Template locally).

Run tests, ensure all pass.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python -m pytest test_gateway.py -k "TestPassthroughProcess" -x -q --tb=short</automated>
  </verify>
  <done>_process_passthrough renders templates correctly with both ${var} and {{var}} syntax, handles missing keys gracefully, flattens nested payloads</done>
</task>

</tasks>

<verification>
cd /Users/haseeb/nix-template/docker && python -m pytest test_gateway.py -k "Watcher or Passthrough" -x -q --tb=short
</verification>

<success_criteria>
- create_watcher/delete_watcher/list_watchers/update_watcher all tested and working
- _process_passthrough handles template rendering with both syntaxes
- Persistence functions (_load/_save) follow existing job engine pattern
- All new tests pass, existing tests unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/16-watcher-engine-real-time-event-subscriptions-with-two-tier-architecture-passthrough-smart-llm/16-01-SUMMARY.md`
</output>
