---
phase: 17-vector-knowledge-base
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - Dockerfile
  - docker/requirements.txt
  - docker/entrypoint.sh
  - .goosehints
  - docker/knowledge/migrate_memory.py
  - docker/test_knowledge.py
autonomous: true
requirements:
  - KB-07
  - KB-08

must_haves:
  truths:
    - "Dockerfile installs chromadb and mcp pip packages"
    - "Entrypoint runs indexer.py on every boot before gateway starts"
    - "Entrypoint runs memory migration on first boot only"
    - ".goosehints no longer references system.md, memory.md, or onboarding.md"
    - ".goosehints includes knowledge base usage instructions"
    - "Knowledge MCP extension is registered in default config.yaml extensions"
    - "Runtime chunks survive container restart (only system collection wiped)"
  artifacts:
    - path: "Dockerfile"
      provides: "chromadb + mcp pip install"
      contains: "chromadb"
    - path: "docker/entrypoint.sh"
      provides: "Knowledge indexer boot hook and memory migration"
      contains: "indexer.py"
    - path: ".goosehints"
      provides: "Slim session context without system.md/memory.md"
      contains: "knowledge_search"
    - path: "docker/knowledge/migrate_memory.py"
      provides: "One-time memory.md to runtime chunks migration"
      exports: ["migrate"]
  key_links:
    - from: "docker/entrypoint.sh"
      to: "docker/knowledge/indexer.py"
      via: "python3 call before gateway start"
      pattern: "python3.*indexer\\.py"
    - from: "docker/entrypoint.sh"
      to: "docker/knowledge/migrate_memory.py"
      via: "python3 call on first boot or when memory.md exists"
      pattern: "migrate_memory\\.py"
    - from: ".goosehints"
      to: "docker/knowledge/server.py"
      via: "knowledge_search/upsert/get instructions in context"
      pattern: "knowledge_search"
---

<objective>
Wire the knowledge base into the deployment pipeline: Dockerfile dependencies, entrypoint boot sequence, .goosehints update, config.yaml extension registration, and memory.md migration.

Purpose: Without integration, the chunker/indexer/server are dead code. This plan makes the vector knowledge base live.
Output: Updated Dockerfile, entrypoint.sh, .goosehints, requirements.txt, and memory migration script.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-vector-knowledge-base-semantic-retrieval-mcp-extension-replacing-monolithic-system-md-context-loading/17-RESEARCH.md
@.planning/phases/17-vector-knowledge-base-semantic-retrieval-mcp-extension-replacing-monolithic-system-md-context-loading/17-01-SUMMARY.md
@.planning/phases/17-vector-knowledge-base-semantic-retrieval-mcp-extension-replacing-monolithic-system-md-context-loading/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memory migration script and tests</name>
  <files>docker/knowledge/migrate_memory.py, docker/test_knowledge.py</files>
  <action>
Create docker/knowledge/migrate_memory.py:
- Read IDENTITY_DIR env var (default "/data/identity"), KNOWLEDGE_DB_PATH env var (default "/data/knowledge/chroma")
- Check if memory.md exists at IDENTITY_DIR/memory.md. If not, print skip message and return
- Parse memory.md by ## sections (same regex split as chunker)
- For each non-empty section, create a runtime chunk:
  - ID: "memory.{slugified-section-title}" (e.g., "memory.integrations")
  - Type: map section titles to types (integrations -> "integration", projects/tools/lessons -> "fact")
  - Metadata: type, source="memory.md", section=title, namespace="runtime", refs="", key=chunk_id
- Upsert each chunk into runtime collection (upsert so re-running is safe/idempotent)
- Print count of migrated sections
- Has a migrate() function and if __name__ == "__main__": migrate() entry point

Add TestMigration (KB-07) to docker/test_knowledge.py (appending to the file created by Plan 17-01):
- Create a temp directory with a sample memory.md containing 2-3 sections (## Integrations, ## Lessons Learned)
- Run migrate() with env vars pointing to temp dirs
- Verify chunks appear in runtime collection with correct types
- Verify re-running migrate() is idempotent (no duplicates)
- Verify missing memory.md is handled gracefully (no error)

Run tests.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -m pytest docker/test_knowledge.py -x -v -k "TestMigration"</automated>
  </verify>
  <done>
    - migrate_memory.py parses memory.md sections into typed runtime chunks
    - Migration is idempotent (upsert, not insert)
    - Missing memory.md handled gracefully
    - TestMigration passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile, entrypoint, .goosehints, and extension wiring</name>
  <files>Dockerfile, docker/requirements.txt, docker/entrypoint.sh, .goosehints</files>
  <action>
1. Update docker/requirements.txt: Add chromadb and mcp[cli] to the requirements list.

2. Update Dockerfile: After the existing `COPY docker/requirements.txt` line, add a pip install step:
   ```
   RUN pip3 install --no-cache-dir --break-system-packages chromadb "mcp[cli]"
   ```
   Place this BEFORE the `COPY docker/ /app/docker/` line for better layer caching. The requirements.txt copy already exists, so put the pip install right after it.

3. Update docker/entrypoint.sh: Add a knowledge base section AFTER the "template version tracking" block (after line ~453) and BEFORE the MOIM section. The section should:

   a. Create knowledge data directory:
   ```bash
   mkdir -p /data/knowledge/chroma
   ```

   b. Run the indexer (system namespace re-index on every boot):
   ```bash
   echo "[knowledge] indexing system knowledge base..."
   runuser -u gooseclaw -- python3 /app/docker/knowledge/indexer.py
   ```

   c. Run memory migration (only if memory.md exists and hasn't been migrated):
   ```bash
   if [ -f "$IDENTITY_DIR/memory.md" ] && [ ! -f "$DATA_DIR/knowledge/.memory_migrated" ]; then
       echo "[knowledge] migrating memory.md to vector store..."
       runuser -u gooseclaw -- python3 /app/docker/knowledge/migrate_memory.py
       touch "$DATA_DIR/knowledge/.memory_migrated"
       echo "[knowledge] memory migration complete"
   fi
   ```

   d. Set KNOWLEDGE_DB_PATH env var:
   ```bash
   export KNOWLEDGE_DB_PATH="/data/knowledge/chroma"
   ```

4. Update docker/entrypoint.sh extensions section: Add knowledge extension to the default extensions block (the `cat >> "$CONFIG_DIR/config.yaml" << 'EXTENSIONS'` heredoc). Add BEFORE the closing EXTENSIONS marker:
   ```yaml
   knowledge:
     enabled: true
     type: stdio
     name: Knowledge
     description: Semantic knowledge base for procedures, integrations, and facts
     cmd: python3
     args:
       - /app/docker/knowledge/server.py
     envs:
       KNOWLEDGE_DB_PATH: /data/knowledge/chroma
     env_keys: []
     timeout: 300
     bundled: null
     available_tools: []
   ```

5. Update .goosehints: Replace the current content with the slim version from RESEARCH.md:
   - REMOVE: @identity-data/system.md, @identity-data/onboarding.md, @identity-data/memory.md lines
   - KEEP: @identity-data/soul.md, @identity-data/user.md
   - KEEP: The CRITICAL onboarding check block at the top, but update it to say "search knowledge base for onboarding flow" instead of referencing onboarding.md directly
   - ADD: Knowledge base usage instructions block explaining knowledge_search(), knowledge_upsert(), knowledge_get()
   - KEEP: Runtime context block (gooseclaw is a personal autonomous AI agent...)

6. Add TestGoosehints (KB-08) to docker/test_knowledge.py:
   - Read .goosehints file
   - Verify it does NOT contain "@identity-data/system.md"
   - Verify it does NOT contain "@identity-data/memory.md"
   - Verify it does NOT contain "@identity-data/onboarding.md"
   - Verify it DOES contain "@identity-data/soul.md"
   - Verify it DOES contain "@identity-data/user.md"
   - Verify it DOES contain "knowledge_search"

Run full test suite across both test files.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && python3 -m pytest docker/test_knowledge.py docker/test_server.py -x -v</automated>
    <manual>Review .goosehints to confirm system.md/memory.md/onboarding.md references removed. Review entrypoint.sh for correct indexer placement (after version tracking, before MOIM). Review Dockerfile for pip install placement.</manual>
  </verify>
  <done>
    - Dockerfile installs chromadb and mcp
    - Entrypoint runs indexer on every boot, migration on first boot
    - .goosehints no longer loads system.md, memory.md, or onboarding.md
    - .goosehints includes knowledge base usage instructions
    - Knowledge extension registered in default config.yaml
    - All tests pass including TestGoosehints
  </done>
</task>

</tasks>

<verification>
- `python3 -m pytest docker/test_knowledge.py docker/test_server.py -x -v` all tests pass
- .goosehints grep: no system.md, no memory.md, no onboarding.md references
- Dockerfile contains `pip3 install` for chromadb
- entrypoint.sh contains indexer.py call before gateway start
- entrypoint.sh contains knowledge extension in default config
</verification>

<success_criteria>
- Full deployment pipeline wired: Dockerfile -> entrypoint -> indexer -> server -> .goosehints
- Memory migration runs once and marks completion
- Token reduction achieved: ~6,000 tokens (system.md + memory.md + onboarding.md) removed from .goosehints
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/17-vector-knowledge-base-semantic-retrieval-mcp-extension-replacing-monolithic-system-md-context-loading/17-03-SUMMARY.md`
</output>
