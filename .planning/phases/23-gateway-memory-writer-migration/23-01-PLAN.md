---
phase: 23-gateway-memory-writer-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/test_gateway.py
autonomous: true
requirements:
  - GW-01
  - GW-02
  - GW-03
  - GW-04

must_haves:
  truths:
    - "Existing TestProcessMemoryExtraction identity tests still pass after refactoring"
    - "New test class verifies mem0.add() is called with correct message format instead of chromadb upsert"
    - "New test class verifies ThreadPoolExecutor timeout behavior (success, timeout, exception)"
    - "New test class verifies IDENTITY_EXTRACT_PROMPT only requests stable traits"
    - "New test class verifies gateway message format converts to mem0 format correctly"
    - "New test class verifies lazy mem0 initialization with thread safety"
  artifacts:
    - path: "docker/test_gateway.py"
      provides: "Updated memory extraction tests + new mem0 integration tests"
      contains: "TestMem0Knowledge|TestMem0AddWithTimeout|TestIdentityExtractPrompt|TestConvertToMem0Messages|TestGetMem0"
  key_links:
    - from: "docker/test_gateway.py::TestMem0Knowledge"
      to: "gateway._mem0_add_knowledge"
      via: "mock mem0.Memory.add"
      pattern: "mock.*add.*called_with"
    - from: "docker/test_gateway.py::TestMem0AddWithTimeout"
      to: "gateway._mem0_add_with_timeout"
      via: "mock ThreadPoolExecutor.submit"
      pattern: "TimeoutError|future.result"
---

<objective>
Prepare test scaffold for gateway memory writer migration from manual ChromaDB extraction to mem0.add().

Purpose: Tests must exist before implementation (Nyquist rule). The existing TestProcessMemoryExtraction tests mock chromadb internals that will be removed. Refactor those to mock mem0 instead, and add new test classes for timeout handling, identity prompt validation, message format conversion, and lazy initialization.

Output: Updated docker/test_gateway.py with 5 new/refactored test classes ready to verify the Phase 23 implementation.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-gateway-memory-writer-migration/23-RESEARCH.md
@.planning/phases/22-mem0-mcp-server-config/22-01-SUMMARY.md
@.planning/phases/22-mem0-mcp-server-config/22-02-SUMMARY.md
@docker/test_gateway.py
@docker/gateway.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor existing memory extraction tests and add mem0 test classes</name>
  <files>docker/test_gateway.py</files>
  <action>
Refactor and add test classes in docker/test_gateway.py. Follow the existing unittest.TestCase + unittest.mock.patch pattern used throughout the file.

**1. Refactor TestProcessMemoryExtraction (existing, ~line 569):**
- Keep all identity-related tests as-is (they test user.md routing which stays)
- Remove or mark as skip any tests that assert chromadb.upsert calls for knowledge items (e.g., test_knowledge_key_prefixed_with_memory, test_knowledge_items_upserted_to_chromadb)
- These will be replaced by TestMem0Knowledge tests below

**2. Add TestConvertToMem0Messages class:**
- Test converting gateway format [{"role": "user", "text": "hello"}] to mem0 format [{"role": "user", "content": "hello"}]
- Test empty messages filtered out
- Test unknown roles mapped to "user"
- Test message text truncated to 2000 chars
- Test empty list returns empty list

**3. Add TestGetMem0 class:**
- Mock `mem0.Memory` and `mem0_config.build_mem0_config` via unittest.mock.patch
- Test lazy initialization: first call creates instance, second returns cached
- Test thread safety: concurrent calls don't create multiple instances
- Test MEM0_TELEMETRY set to "false" in os.environ during init
- Test init failure (exception) returns None and logs error

**4. Add TestMem0Knowledge class (replaces chromadb knowledge tests):**
- Mock `_get_mem0()` to return a mock Memory instance
- Test _mem0_add_knowledge calls m.add(messages=..., user_id=...) with correctly converted messages
- Test _mem0_add_knowledge skips when mem0 is None (not available)
- Test _mem0_add_knowledge skips when messages list is empty

**5. Add TestMem0AddWithTimeout class:**
- Mock `_get_mem0_executor()` to return a mock ThreadPoolExecutor
- Test successful add returns result within timeout
- Test TimeoutError after N seconds returns None and logs
- Test generic Exception returns None and logs

**6. Add TestIdentityExtractPrompt class:**
- Import IDENTITY_EXTRACT_PROMPT from gateway module
- Assert prompt contains "IDENTITY" or "identity" (not "knowledge")
- Assert prompt mentions "6+ months" or "stable" for trait duration
- Assert prompt's DO NOT section excludes projects, deadlines, integrations
- Assert prompt requests JSON output with "identity" key

**7. Add TestProcessIdentityExtraction class:**
- Mock the identity file path and user.md content
- Test identity items routed to correct user.md sections via _classify_identity_section
- Test duplicate identity items skipped via _fact_already_exists
- Test empty extraction ({"empty": true}) does nothing
- (This is essentially the existing identity tests from TestProcessMemoryExtraction, verified to work with the renamed function)

**Mock pattern:** Follow the existing test_memory_server.py approach. For mem0, create a mock Memory class:
```python
mock_memory = MagicMock()
mock_memory.add.return_value = {"results": [{"id": "test", "event": "ADD"}]}
```

For ThreadPoolExecutor timeout tests:
```python
mock_future = MagicMock()
mock_future.result.side_effect = TimeoutError()
```

**Important:** All new test classes must be importable and runnable even before the gateway.py implementation changes. Use `unittest.mock.patch` to mock functions that don't exist yet (like `_mem0_add_knowledge`, `_get_mem0`). The tests will initially fail (RED state) and pass after Plan 02 implements the functions.

Mark tests that depend on not-yet-implemented functions with `@unittest.skip("Waiting for Plan 23-02 implementation")` so the test suite stays green.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python -m pytest test_gateway.py -x -k "TestProcessMemoryExtraction or TestConvertToMem0Messages or TestGetMem0 or TestMem0Knowledge or TestMem0AddWithTimeout or TestIdentityExtractPrompt or TestProcessIdentityExtraction" --timeout=30 -v 2>&1 | tail -30</automated>
    <manual>Verify no existing tests broken by the refactoring</manual>
  </verify>
  <done>
    - TestProcessMemoryExtraction identity tests still pass
    - TestConvertToMem0Messages has 4+ test methods
    - TestGetMem0 has 3+ test methods
    - TestMem0Knowledge has 3+ test methods
    - TestMem0AddWithTimeout has 3+ test methods
    - TestIdentityExtractPrompt has 3+ test methods
    - TestProcessIdentityExtraction has 3+ test methods
    - Full test suite passes: `python -m pytest test_gateway.py --timeout=30`
  </done>
</task>

</tasks>

<verification>
- `cd /Users/haseeb/nix-template/docker && python -m pytest test_gateway.py --timeout=30 -v` -- full suite green
- `cd /Users/haseeb/nix-template/docker && python -m pytest test_gateway.py -x -k "Memory or Mem0 or Identity or Extract" --timeout=30` -- all memory-related tests pass
- No test imports fail (all mocks properly set up)
</verification>

<success_criteria>
- 5+ new test classes added to test_gateway.py
- Existing identity extraction tests preserved and passing
- Old chromadb knowledge tests refactored or skipped
- Test suite remains fully green
- Tests are structured to verify GW-01 through GW-04 once implementation lands
</success_criteria>

<output>
After completion, create `.planning/phases/23-gateway-memory-writer-migration/23-01-SUMMARY.md`
</output>
