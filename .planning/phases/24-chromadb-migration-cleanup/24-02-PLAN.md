---
phase: 24-chromadb-migration-cleanup
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - docker/knowledge/server.py
  - docker/knowledge/indexer.py
  - docker/test_server.py
  - docker/test_knowledge.py
  - docker/test_gateway.py
autonomous: true
requirements: [MIG-03]

must_haves:
  truths:
    - "knowledge/server.py only operates on the system collection (no runtime_col references)"
    - "knowledge_upsert tool is removed from the knowledge MCP server"
    - "knowledge_delete tool is removed from the knowledge MCP server"
    - "knowledge_search only queries system_col"
    - "knowledge_get only queries system_col"
    - "knowledge_recent only queries system_col"
    - "indexer.py no longer calls get_or_create_collection('runtime')"
    - "All tests pass after cleanup (no references to removed functionality)"
  artifacts:
    - path: "docker/knowledge/server.py"
      provides: "System-only knowledge MCP server"
      min_lines: 80
    - path: "docker/knowledge/indexer.py"
      provides: "System-only indexer (no runtime ensure)"
      min_lines: 30
    - path: "docker/test_server.py"
      provides: "Tests for system-only knowledge server"
      min_lines: 100
    - path: "docker/test_gateway.py"
      provides: "Clean gateway tests (dead chromadb tests removed)"
  key_links:
    - from: "docker/knowledge/server.py"
      to: "chromadb system collection"
      via: "get_or_create_collection('system')"
      pattern: "system_col"
    - from: "docker/test_server.py"
      to: "docker/knowledge/server.py"
      via: "test assertions match system-only API"
      pattern: "knowledge_search|knowledge_get|knowledge_recent"
---

<objective>
Narrow knowledge/server.py to system-only, remove runtime collection references from indexer.py, and update all affected tests to match the new system-only architecture.

Purpose: MIG-03 (runtime collection deprecated, system collection untouched). After this plan, the knowledge MCP server handles only system docs, and mem0 handles all user memories.
Output: System-only server.py, cleaned indexer.py, updated test files
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-chromadb-migration-cleanup/24-RESEARCH.md
@.planning/phases/24-chromadb-migration-cleanup/24-01-SUMMARY.md
@docker/knowledge/server.py
@docker/knowledge/indexer.py
@docker/test_server.py
@docker/test_knowledge.py
@docker/test_gateway.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow knowledge/server.py to system-only and clean indexer.py</name>
  <files>docker/knowledge/server.py, docker/knowledge/indexer.py</files>
  <action>
Modify docker/knowledge/server.py to remove all runtime collection references (per 24-RESEARCH.md Pattern 3):

1. REMOVE the runtime_col initialization line: `runtime_col = client.get_or_create_collection("runtime", ...)`. Keep system_col.
2. REMOVE the entire knowledge_upsert tool function (it was runtime-only, replaced by mem0 memory_add)
3. REMOVE the entire knowledge_delete tool function (it was runtime-only, replaced by mem0 memory_delete)
4. MODIFY knowledge_search: remove any logic that queries runtime_col. Only query system_col. If the function currently queries both collections and merges results, narrow it to system_col only.
5. MODIFY knowledge_get: remove any runtime_col fallback. Only look up from system_col.
6. MODIFY knowledge_recent: remove any runtime_col query. Only query system_col.
7. Clean up any imports or variables that are now unused after removing runtime references.

Modify docker/knowledge/indexer.py:
1. REMOVE the line that calls `client.get_or_create_collection("runtime")` (approximately line 37 per research). The indexer only needs the system collection.
2. Keep everything else in the indexer unchanged (system collection indexing logic stays).

Do NOT delete the runtime collection from ChromaDB. Do NOT modify gateway.py (already cleaned in Phase 23). Do NOT modify migrate_memory.py (historical, still needed for the migration chain).
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template && grep -n "runtime" docker/knowledge/server.py docker/knowledge/indexer.py | grep -v "^Binary" | head -20; echo "---"; grep -c "knowledge_upsert\|knowledge_delete" docker/knowledge/server.py || echo "0 matches (good)"</automated>
    <manual>Verify server.py has no runtime_col references, no knowledge_upsert, no knowledge_delete</manual>
  </verify>
  <done>server.py is system-only (zero runtime_col refs, zero knowledge_upsert, zero knowledge_delete). indexer.py no longer ensures runtime collection exists.</done>
</task>

<task type="auto">
  <name>Task 2: Update all test files to match system-only architecture</name>
  <files>docker/test_server.py, docker/test_knowledge.py, docker/test_gateway.py</files>
  <action>
Update docker/test_server.py:
1. In the _ServerTestBase (or equivalent base class), remove runtime_col from setUp/tearDown. The test setup should only create/use a system collection.
2. REMOVE TestKnowledgeUpsert test class entirely (tests removed tool)
3. REMOVE TestKnowledgeDelete test class entirely (tests removed tool)
4. UPDATE TestKnowledgeSearch: remove any test methods that verify runtime collection searching. Keep tests for system collection search.
5. UPDATE TestKnowledgeGet: remove runtime_col fallback tests. Keep system_col lookup tests.
6. UPDATE TestKnowledgeRecent: remove runtime_col query tests. Keep system_col tests.
7. Ensure all remaining tests reference only system_col in their setup/assertions.

Update docker/test_knowledge.py:
1. In TestIndexer class: remove test_runtime_collection_preserved and test_runtime_collection_created_if_missing (per research Wave 0 gaps). These test behavior that no longer exists.
2. Keep all other indexer tests (system collection tests, file processing tests, etc.)
3. Keep the TestMigrateMemory tests (historical migration, still runs)
4. Keep the TestMem0Migration tests added in Plan 24-01

Update docker/test_gateway.py:
1. REMOVE the skipped chromadb tests at approximately lines 715-819 (8 tests skipped by Phase 23). These are fully dead code now. The skip decorators say they're waiting for removal. This is that removal.
2. Keep all other gateway tests (mem0 tests from Phase 23, identity tests, etc.)

After all changes, run the full test suite to verify nothing is broken.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python -m pytest test_server.py test_knowledge.py test_gateway.py -x --timeout=30 -v 2>&1 | tail -20</automated>
    <manual>Verify no test references runtime_col, knowledge_upsert, or knowledge_delete in non-migration contexts</manual>
  </verify>
  <done>All test files updated: no runtime_col in test_server.py setup, no TestKnowledgeUpsert/TestKnowledgeDelete classes, no skipped chromadb tests in test_gateway.py, no stale indexer tests in test_knowledge.py. Full suite green.</done>
</task>

</tasks>

<verification>
1. No runtime references in server.py: `grep -c "runtime" docker/knowledge/server.py` returns 0
2. No runtime ensure in indexer.py: `grep -c "runtime" docker/knowledge/indexer.py` returns 0
3. No knowledge_upsert/delete in server.py: `grep -c "knowledge_upsert\|knowledge_delete" docker/knowledge/server.py` returns 0
4. Full test suite passes: `cd docker && python -m pytest test_server.py test_knowledge.py test_gateway.py -x --timeout=30`
5. No skipped chromadb tests remain in test_gateway.py: `grep -c "skip.*chromadb\|skip.*knowledge.*upsert" docker/test_gateway.py` returns 0
</verification>

<success_criteria>
- knowledge/server.py is system-only: only system_col, only knowledge_search/get/recent tools
- knowledge/indexer.py does not reference or create runtime collection
- All tests updated to match new system-only architecture
- Full test suite green with zero references to removed functionality
- Dead skipped chromadb tests removed from test_gateway.py
</success_criteria>

<output>
After completion, create `.planning/phases/24-chromadb-migration-cleanup/24-02-SUMMARY.md`
</output>
