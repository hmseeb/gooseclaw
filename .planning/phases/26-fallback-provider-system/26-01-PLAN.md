---
phase: 26-fallback-provider-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/tests/test_fallback.py
  - docker/gateway.py
autonomous: true
requirements:
  - FB-01
  - FB-04
  - FB-05
  - FB-09

must_haves:
  truths:
    - "Retriable errors (429, 5xx, timeout, connection) are classified correctly"
    - "Permanent errors (401, 403, 400) are NOT classified as retriable"
    - "Fallback provider config validates provider name, model presence, and known provider"
    - "Fallback config round-trips through save/load without data loss"
    - "Primary provider is always attempted first before any fallback"
  artifacts:
    - path: "docker/tests/test_fallback.py"
      provides: "Test scaffold for fallback system"
      min_lines: 100
    - path: "docker/gateway.py"
      provides: "Error classification function and validation extension"
      contains: "_is_retriable_provider_error"
  key_links:
    - from: "docker/gateway.py"
      to: "docker/gateway.py::validate_setup_config"
      via: "fallback_providers validation block"
      pattern: "fallback_providers.*must be"
---

<objective>
Add the fallback engine foundation: error classification, config validation, config persistence, and comprehensive test scaffold.

Purpose: Without error classification, the system cannot distinguish "try another provider" from "stop, the API key is wrong". Without validation, users could save broken fallback configs. Tests ensure correctness before wiring into live relay paths.

Output: `_is_retriable_provider_error()` function, extended `validate_setup_config()`, and `docker/tests/test_fallback.py` with passing tests.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-fallback-provider-system/26-RESEARCH.md
@docker/gateway.py (lines 2153-2250: validate_setup_config, lines 7282-7295: _is_fatal_provider_error)
@docker/tests/test_setup.py (test patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffold for fallback system</name>
  <files>docker/tests/test_fallback.py</files>
  <action>
Create `docker/tests/test_fallback.py` with these test classes:

**TestErrorClassification:**
- `test_retriable_429_rate_limit` - "429" in error string returns True
- `test_retriable_500_server_error` - "500", "502", "503", "504", "529" each return True
- `test_retriable_timeout` - "timeout", "timed out", "took too long" each return True
- `test_retriable_connection_error` - "connection refused", "connection reset", "connection error" return True
- `test_not_retriable_auth_401` - "401" returns False
- `test_not_retriable_forbidden_403` - "403" returns False
- `test_not_retriable_bad_request_400` - "400 bad request" returns False
- `test_not_retriable_empty_string` - empty string returns False
- `test_not_retriable_none` - None returns False
- `test_not_retriable_broken_pipe` - "broken pipe" returns False (handled by existing _is_fatal_provider_error)

**TestFallbackValidation:**
- `test_valid_fallback_providers` - valid array of {provider, model} dicts passes validation
- `test_fallback_providers_not_array` - non-array returns error
- `test_fallback_provider_missing_provider` - missing "provider" key returns error
- `test_fallback_provider_unknown_provider` - unknown provider name returns error
- `test_fallback_provider_missing_model` - missing "model" key returns error
- `test_mem0_fallback_providers_validates_same` - same rules apply to `mem0_fallback_providers`
- `test_empty_fallback_array_valid` - empty array is valid (user chose no fallbacks)

**TestFallbackPersistence:**
- `test_fallback_config_roundtrip` - save setup.json with `fallback_providers` array, load it back, array matches exactly
- `test_mem0_fallback_config_roundtrip` - same for `mem0_fallback_providers`

**TestPrimaryRestore:**
- `test_primary_always_first` - verify the fallback chain logic tries primary before fallbacks (unit test the function signature/contract, actual wiring tested in plan 03)

Import the functions directly from gateway module. Use the existing test pattern from `test_setup.py`: import from `conftest.py`, use `tmp_path` fixtures for config file isolation. Add `sys.path.insert` for docker dir if needed.

Tests should initially FAIL (functions don't exist yet). That's expected for Task 2.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_fallback.py --collect-only 2>&1 | grep "test session starts"</automated>
    <manual>Verify test file collects all test cases without import errors</manual>
  </verify>
  <done>test_fallback.py exists with all test classes and methods, collects successfully via pytest</done>
</task>

<task type="auto">
  <name>Task 2: Implement error classification, validation extension, and config persistence</name>
  <files>docker/gateway.py</files>
  <action>
**1. Add `_is_retriable_provider_error()` function** (near line 7282, next to `_is_fatal_provider_error()`):

```python
_RETRIABLE_STATUS_CODES = {"429", "500", "502", "503", "504", "529"}

def _is_retriable_provider_error(error_string):
    """Check if error should trigger fallback to next provider.

    Retriable: rate limits (429), server errors (5xx), timeouts, connection errors.
    NOT retriable: auth errors (401/403), bad requests (400), broken pipe (needs restart).
    """
    if not error_string:
        return False
    low = error_string.lower()
    for code in _RETRIABLE_STATUS_CODES:
        if code in error_string:
            return True
    if "timeout" in low or "timed out" in low or "took too long" in low:
        return True
    if "connection" in low and any(w in low for w in ("refused", "reset", "error", "aborted")):
        return True
    return False
```

**2. Extend `validate_setup_config()`** (after the models array validation block, around line 2250):

Add validation for `fallback_providers` and `mem0_fallback_providers` fields. For each:
- Must be an array if present
- Each element must be a dict with "provider" (must be in `env_map`) and "model" (non-empty string)
- Empty array is valid (no fallbacks configured)

Follow the exact pattern from the research code examples section.

**3. Add `fallback_providers` and `mem0_fallback_providers` to the string field max-length guard** (line 2199) -- skip, these are arrays not strings. But DO add them to the body of `apply_config()` / `handle_save()` so they pass through and persist. Check if `handle_save()` already stores arbitrary keys from the config dict or if specific fields are extracted. If specific fields are extracted, add the two new fields.

Verify the save/load path: `handle_save()` at line 9410 calls `validate_setup_config()` then `save_setup()`. Check if `save_setup()` stores the full config dict or cherry-picks fields. If cherry-picks, add the fallback fields.

Make sure `_is_retriable_provider_error` is importable from the test file.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_fallback.py -x -q --timeout=30</automated>
    <manual>All test_fallback.py tests pass</manual>
  </verify>
  <done>Error classification correctly identifies retriable vs permanent errors, validation catches malformed fallback configs, fallback arrays persist through save/load cycle</done>
</task>

</tasks>

<verification>
- `cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_fallback.py -x -q --timeout=30` passes all tests
- `cd /Users/haseeb/nix-template/docker && python -m pytest tests/ -x -q --timeout=30` full suite still passes (no regressions)
</verification>

<success_criteria>
- _is_retriable_provider_error() correctly classifies 429, 5xx, timeout, connection errors as retriable
- _is_retriable_provider_error() correctly rejects 401, 403, 400, broken pipe, None, empty string
- validate_setup_config() validates fallback_providers and mem0_fallback_providers arrays
- Fallback config arrays round-trip through save_setup/load_setup
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-fallback-provider-system/26-01-SUMMARY.md`
</output>
