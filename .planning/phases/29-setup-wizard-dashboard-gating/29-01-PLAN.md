---
phase: 29-setup-wizard-dashboard-gating
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - docker/tests/test_voice.py
  - docker/tests/test_setup.py
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-04, UI-07]

must_haves:
  truths:
    - "test_voice.py contains TestVaultWrite class with save/read roundtrip test"
    - "test_voice.py contains TestVoicePageGating class with auth gating and key gating tests"
    - "test_setup.py contains TestGeminiKeyInSetup class with gemini_api_key handling in save config and gemini_api_key_set in get_config"
    - "All new tests fail (RED) because implementation doesn't exist yet"
  artifacts:
    - path: "docker/tests/test_voice.py"
      provides: "Vault write and voice page gating tests"
      contains: "TestVaultWrite"
    - path: "docker/tests/test_setup.py"
      provides: "Gemini key setup config tests"
      contains: "TestGeminiKeyInSetup"
  key_links:
    - from: "docker/tests/test_voice.py"
      to: "docker/gateway.py"
      via: "imports _save_vault_key, _get_gemini_api_key"
      pattern: "_save_vault_key|_get_gemini_api_key"
    - from: "docker/tests/test_setup.py"
      to: "docker/gateway.py"
      via: "HTTP requests to /api/setup/save and /api/setup/config"
      pattern: "/api/setup/save|/api/setup/config"
---

<objective>
Write RED tests for all Phase 29 requirements before implementation.

Purpose: TDD scaffolding. Tests define the contract for vault write, voice page gating, and Gemini key in setup config. All tests MUST fail initially (RED phase) since _save_vault_key(), /voice route, and gemini_api_key handling don't exist yet.

Output: Extended test_voice.py with TestVaultWrite and TestVoicePageGating classes. Extended test_setup.py with TestGeminiKeyInSetup class.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-setup-wizard-dashboard-gating/29-RESEARCH.md
@docker/tests/test_voice.py
@docker/tests/test_setup.py
@docker/tests/conftest.py
@docker/gateway.py (lines 8440-8520: VAULT_FILE, _get_gemini_api_key)
@docker/gateway.py (lines 9757-9840: handle_admin_page, handle_get_config patterns)
@docker/gateway.py (lines 9841-9946: handle_save)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vault write + voice page gating tests to test_voice.py</name>
  <files>docker/tests/test_voice.py</files>
  <action>
Append two new test classes to docker/tests/test_voice.py:

**TestVaultWrite** (covers SETUP-04):
- Import `_save_vault_key` from gateway (add to the existing import block at top). This import WILL fail initially since the function doesn't exist yet. That's expected for RED phase. To handle this gracefully, use a try/except ImportError around the import OR use `getattr(gateway, '_save_vault_key', None)` in test body with pytest.skip if None.
- `test_save_and_read_roundtrip`: Call `_save_vault_key("GEMINI_API_KEY", "test-key-123")`, then call `_get_gemini_api_key()`, assert result == "test-key-123". Must patch `gateway.VAULT_FILE` to a temp path (VAULT_FILE is module-level, computed from DATA_DIR at import time, so the conftest DATA_DIR patch doesn't cover it).
- `test_save_overwrites_existing`: Save key "old", save key "new", read back, assert "new".
- `test_save_preserves_other_keys`: Write a vault.yaml with `OTHER_KEY: foo` first, then call `_save_vault_key("GEMINI_API_KEY", "bar")`, read back vault.yaml and assert both keys exist.
- `test_save_creates_directory`: Set VAULT_FILE to a path inside a non-existent directory, call _save_vault_key, assert file was created.

**TestVoicePageGating** (covers SETUP-02, UI-07):
- These are HTTP-level tests using the `live_gateway` and `auth_session` fixtures.
- `test_voice_requires_auth`: GET /voice without auth cookie, expect 302 redirect to /login.
- `test_voice_no_key_shows_gate`: GET /voice with auth cookie but no Gemini key in vault, expect 200 with HTML containing "Configure Gemini" or link to /setup.
- `test_voice_with_key_serves_page`: Write GEMINI_API_KEY to vault.yaml in the test data dir, GET /voice with auth, expect 200 with content (either voice.html or a "coming soon" placeholder since voice.html doesn't exist yet in Phase 29).

CRITICAL: For vault tests that use `_save_vault_key`, patch `gateway.VAULT_FILE` to a temp file using `monkeypatch.setattr` or `unittest.mock.patch`. The live_gateway fixture patches DATA_DIR but VAULT_FILE is computed at import time and won't change.

For HTTP tests: the live_gateway fixture creates an empty vault.yaml at `secrets_dir`. But the module's VAULT_FILE still points to the original computed path. The HTTP test handler reads VAULT_FILE to check for Gemini key. You need to also patch `gateway.VAULT_FILE` to point to the test data's vault.yaml. Add a fixture or use monkeypatch in each test.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_voice.py::TestVaultWrite tests/test_voice.py::TestVoicePageGating -x -q 2>&1 | tail -20</automated>
    <manual>Tests should FAIL (RED) with ImportError for _save_vault_key or 404 for /voice route. This confirms the test contract is written but implementation is missing.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>TestVaultWrite has 4 tests covering save/read roundtrip, overwrite, preserve other keys, and directory creation. TestVoicePageGating has 3 tests covering auth required, gate page without key, and page with key. All tests reference _save_vault_key and /voice route which don't exist yet, so they fail (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Add Gemini key setup config tests to test_setup.py</name>
  <files>docker/tests/test_setup.py</files>
  <action>
Append a new test class TestGeminiKeyInSetup to docker/tests/test_setup.py:

**TestGeminiKeyInSetup** (covers SETUP-01):
- `test_save_with_gemini_key_writes_vault`: POST /api/setup/save with a valid config that includes `gemini_api_key: "AIzaSy-test-key-123"`. After save, read vault.yaml from the test data dir and assert GEMINI_API_KEY is present. Must patch `gateway.VAULT_FILE` to point to test data's secrets/vault.yaml.
- `test_save_without_gemini_key_skips_vault`: POST /api/setup/save with a valid config that has no gemini_api_key field. Vault should remain unchanged (no GEMINI_API_KEY written).
- `test_get_config_includes_gemini_key_set_true`: Write GEMINI_API_KEY to vault.yaml, GET /api/setup/config, assert response contains `gemini_api_key_set: true`.
- `test_get_config_includes_gemini_key_set_false`: Ensure vault has no GEMINI_API_KEY, GET /api/setup/config, assert response contains `gemini_api_key_set: false`.
- `test_reconfigure_blank_gemini_keeps_existing`: Save config with gemini_api_key first. Then save again with gemini_api_key as empty string and config containing gemini_api_key_set indicator. Vault should still have the original key (reconfigure doesn't clobber).

For all tests, use the `live_gateway` and `auth_session` fixtures. The save tests need a valid config payload with at minimum: `provider_type`, `web_auth_token` (or existing auth). Follow the pattern from existing test_setup.py tests.

CRITICAL: Patch `gateway.VAULT_FILE` to point to the test data's vault.yaml path. Since live_gateway patches DATA_DIR but VAULT_FILE is computed at import time, you must explicitly patch it.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_setup.py::TestGeminiKeyInSetup -x -q 2>&1 | tail -20</automated>
    <manual>Tests should FAIL (RED) because handle_save doesn't extract gemini_api_key, handle_get_config doesn't add gemini_api_key_set, and _save_vault_key doesn't exist yet.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>TestGeminiKeyInSetup has 5 tests covering vault write on save, vault skip when absent, gemini_api_key_set true/false in config response, and reconfigure preservation. All tests fail (RED) since implementation is missing.</done>
</task>

</tasks>

<verification>
All new tests are in RED state (failing). This confirms:
1. Test contracts are written for all 4 requirements (SETUP-01, SETUP-02, SETUP-04, UI-07)
2. No implementation exists yet (expected)
3. Tests will go GREEN when Plans 02 and 03 implement the functionality
Run: `cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_voice.py::TestVaultWrite tests/test_voice.py::TestVoicePageGating tests/test_setup.py::TestGeminiKeyInSetup -x -q`
</verification>

<success_criteria>
- TestVaultWrite class exists with 4 test methods
- TestVoicePageGating class exists with 3 test methods
- TestGeminiKeyInSetup class exists with 5 test methods
- All 12 tests fail (RED) because implementation is missing
- Existing tests in test_voice.py still pass (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/29-setup-wizard-dashboard-gating/29-01-SUMMARY.md`
</output>
