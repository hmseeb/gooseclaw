---
phase: 29-setup-wizard-dashboard-gating
plan: 03
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - docker/setup.html
autonomous: true
requirements: [SETUP-01]

must_haves:
  truths:
    - "User sees a Gemini API Key field in setup wizard step 3 (Optional Settings)"
    - "Field has password toggle visibility matching existing secret field pattern"
    - "Field shows format validation on blur (too short warning)"
    - "saveConfig() includes gemini_api_key in the config payload"
    - "On reconfigure, blank gemini field preserves existing key (matches groq/telegram pattern)"
    - "Reconfigure mode shows placeholder dots when key is already set"
  artifacts:
    - path: "docker/setup.html"
      provides: "Gemini API key input field in setup wizard"
      contains: "geminiApiKey"
  key_links:
    - from: "setup.html geminiApiKey input"
      to: "setup.html saveConfig()"
      via: "getElementById('geminiApiKey').value collected into config object"
      pattern: "geminiApiKey"
    - from: "setup.html saveConfig()"
      to: "gateway.py handle_save()"
      via: "POST /api/setup/save with config.gemini_api_key"
      pattern: "gemini_api_key"
---

<objective>
Add Gemini API key input field to the setup wizard's optional settings step.

Purpose: Users can enter their Gemini API key in the setup wizard UI. The key is sent to the backend via the existing save flow, where gateway.py writes it to vault (implemented in Plan 02). This is the frontend half of SETUP-01.

Output: setup.html with Gemini API key field in step 3, validation, save integration, and reconfigure support.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-setup-wizard-dashboard-gating/29-RESEARCH.md
@docker/setup.html (lines 1660-1700: groq key field pattern in step 3)
@docker/setup.html (lines 4990-5000: reconfigure pre-fill for groq key)
@docker/setup.html (lines 5380-5470: saveConfig function, groq key handling)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gemini API key field to setup wizard step 3</name>
  <files>docker/setup.html</files>
  <action>
Three changes to setup.html:

**1. Add Gemini API Key input field in Step 3 (Optional Settings)**

After the Groq API key field block (after the groqExtractionKey field and its field-hint div, around line 1685), add:

```html
<div class="field">
  <label>Gemini API Key <span style="color:var(--muted);font-size:12px">(optional, for voice)</span></label>
  <div class="secret-field-wrapper">
    <input type="password" id="geminiApiKey" placeholder="AI..." onblur="validateGeminiFormat(this)">
    <button type="button" class="secret-toggle-btn" onclick="toggleSecretVisibility(this)" title="Toggle visibility">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
    </button>
  </div>
  <div class="field-hint">enables voice dashboard. get key at <a href="https://aistudio.google.com/apikey" target="_blank" style="color:var(--accent)">aistudio.google.com</a></div>
  <div id="geminiFormatError" style="display:none;font-size:12px;color:var(--error);margin-top:4px"></div>
</div>
```

Follow the EXACT HTML structure and class names of the groqExtractionKey field. Use the same `secret-field-wrapper`, `secret-toggle-btn`, and eye SVG pattern.

**2. Add validateGeminiFormat() function and reconfigure pre-fill**

Near the `validateGroqFormat()` function (search for it in the JS section), add:

```javascript
function validateGeminiFormat(el) {
  const err = document.getElementById('geminiFormatError');
  const val = el.value.trim();
  if (!val) { err.style.display = 'none'; return; }
  if (val.length < 10) {
    err.textContent = 'API key appears too short';
    err.style.display = 'block';
  } else {
    err.style.display = 'none';
  }
}
```

In the reconfigure pre-fill section (near the groq pre-fill code around line 4994), add:

```javascript
/* pre-fill gemini api key placeholder */
const geminiEl = document.getElementById('geminiApiKey');
if (geminiEl && config.gemini_api_key_set) {
  geminiEl.value = '';
  geminiEl.placeholder = '\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022 (clear and re-enter to change)';
}
```

**3. Add gemini_api_key to saveConfig()**

In the `saveConfig()` function, after the line that sets `groq_extraction_key` (around line 5388), add:

```javascript
gemini_api_key: document.getElementById('geminiApiKey')?.value || '',
```

Then, after the groq_extraction_key reconfigure preservation block (around line 5462), add:

```javascript
/* handle gemini api key: keep existing if blank during reconfigure */
if (!config.gemini_api_key && isReconfigure && dashboardConfig.gemini_api_key_set) {
  delete config.gemini_api_key;
}
```

This follows the EXACT same pattern as groq_extraction_key handling: if the field is blank during reconfigure and the key was already set, delete the field from config so the backend preserves the existing vault value.
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && grep -c "geminiApiKey" setup.html</automated>
    <manual>Open setup.html in browser. Navigate to step 3. Verify Gemini API Key field appears after Groq key. Verify password toggle works. Verify blur validation shows error for short input. Verify saveConfig sends gemini_api_key in payload (check Network tab).</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>setup.html step 3 contains Gemini API Key field with password toggle, blur validation, aistudio link hint. saveConfig() includes gemini_api_key in payload. Reconfigure mode preserves existing key when field is blank. grep finds 5+ occurrences of "geminiApiKey" in setup.html.</done>
</task>

</tasks>

<verification>
1. `grep -c "geminiApiKey" docker/setup.html` returns 5+
2. `grep -c "validateGeminiFormat" docker/setup.html` returns 2+
3. `grep -c "gemini_api_key" docker/setup.html` returns 3+
4. Full test suite still passes: `cd /Users/haseeb/nix-template/docker && python -m pytest tests/ -x -q --ignore=tests/e2e`
</verification>

<success_criteria>
- Gemini API Key field visible in step 3 of setup wizard
- Field uses password input type with toggle visibility button
- validateGeminiFormat shows "too short" warning for keys under 10 chars
- saveConfig includes gemini_api_key in the POST payload
- Reconfigure mode shows bullet placeholder when key already set
- Blank field on reconfigure preserves existing key (delete from payload)
- Link to aistudio.google.com/apikey in field hint
</success_criteria>

<output>
After completion, create `.planning/phases/29-setup-wizard-dashboard-gating/29-03-SUMMARY.md`
</output>
