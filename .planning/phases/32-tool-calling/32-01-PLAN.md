---
phase: 32-tool-calling
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - docker/gateway.py
  - docker/tests/test_voice.py
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-05, TOOL-06]
must_haves:
  truths:
    - "_discover_voice_tools() queries goosed /config and returns Gemini function declarations for all enabled extensions"
    - "_voice_execute_tool() relays a natural language prompt to goosed via _do_rest_relay and returns the result"
    - "_voice_build_tool_response() builds correct toolResponse JSON with function ID, name, result, and SILENT scheduling"
    - "_gemini_build_config() accepts an optional tools parameter and includes functionDeclarations in setup config"
    - "Extension names with special characters are sanitized to Gemini-compatible [a-zA-Z_][a-zA-Z0-9_]* format"
    - "A name mapping dict preserves the original extension name for routing"
    - "Tool discovery returns empty list gracefully when goosed is not ready"
  artifacts:
    - path: "docker/gateway.py"
      provides: "_discover_voice_tools, _voice_execute_tool, _voice_send_tool_response functions"
      contains: "_discover_voice_tools"
    - path: "docker/tests/test_voice.py"
      provides: "TestToolDiscovery, TestToolExecution, TestToolResponse test classes"
      contains: "TestToolDiscovery"
  key_links:
    - from: "_discover_voice_tools"
      to: "goosed /config"
      via: "_goosed_conn GET /config"
      pattern: "_goosed_conn.*GET.*config"
    - from: "_voice_execute_tool"
      to: "_do_rest_relay"
      via: "function call with prompt and session_id"
      pattern: "_do_rest_relay"
    - from: "_gemini_build_config"
      to: "Gemini setup config"
      via: "tools parameter in config dict"
      pattern: "functionDeclarations"
---

<objective>
Implement and test the three core tool-calling functions: discovery, execution, and response. Also update `_gemini_build_config` to accept tools.

Purpose: These pure functions are the building blocks that Plan 02 wires into the relay loop. TDD ensures correct formats before integration.
Output: Tested `_discover_voice_tools()`, `_voice_execute_tool()`, `_voice_send_tool_response()`, and updated `_gemini_build_config()`.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-tool-calling/32-RESEARCH.md
@docker/gateway.py (lines 6843-6856 for goosed /config pattern, 6960-6987 for _create_goose_session, 7393-7465 for _do_rest_relay, 8966-8998 for _gemini_build_config, 9025-9048 for _voice_parse_server_message)
@docker/tests/test_voice.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Write failing tests for tool discovery, execution, and response</name>
  <files>docker/tests/test_voice.py</files>
  <action>
Add three new test classes to the existing test_voice.py file. Import the new functions (they won't exist yet, so use try/except or conditional imports that skip gracefully).

**TestToolDiscovery:**
- `test_returns_declarations_for_enabled_extensions`: Mock `_goosed_conn` to return a /config response with 2 enabled extensions (e.g., "google-calendar", "mem0"). Assert `_discover_voice_tools()` returns a list of 2 function declarations. Each declaration must have `name`, `description`, and `parameters` (with `request` STRING property).
- `test_sanitizes_extension_names`: Mock /config with extension name "google-calendar" (has hyphen). Assert returned declaration name is "google_calendar" (hyphen replaced with underscore).
- `test_returns_name_mapping`: Assert `_discover_voice_tools()` returns `(declarations, name_map)` tuple where `name_map["google_calendar"] == "google-calendar"`.
- `test_skips_disabled_extensions`: Mock /config with one enabled and one disabled (`"enabled": false`) extension. Assert only 1 declaration returned.
- `test_returns_empty_on_goosed_error`: Mock `_goosed_conn` to raise ConnectionError. Assert returns `([], {})`.
- `test_returns_empty_on_non_200`: Mock /config returning 500. Assert returns `([], {})`.

**TestToolExecution:**
- `test_executes_via_do_rest_relay`: Mock `_do_rest_relay` to return `("Calendar event found", "", [])`. Call `_voice_execute_tool("google_calendar", {"request": "check my schedule"}, "session-123", "google-calendar")`. Assert result is `{"result": "Calendar event found"}`.
- `test_truncates_long_results`: Mock `_do_rest_relay` returning a 5000-char string. Assert result["result"] is max 2000 chars.
- `test_returns_error_on_relay_failure`: Mock `_do_rest_relay` returning `("", "goosed timeout", [])`. Assert result is `{"error": "goosed timeout"}`.
- `test_returns_error_on_exception`: Mock `_do_rest_relay` raising Exception. Assert result has "error" key.

**TestToolResponse:**
- `test_builds_correct_json_structure`: Call `_voice_build_tool_response("call-123", "google_calendar", {"result": "Event at 3pm"})`. Assert it returns dict with `toolResponse.functionResponses[0]` containing `id`, `name`, and `response` fields.
- `test_response_has_correct_fields`: Verify id="call-123", name="google_calendar", response={"result": "Event at 3pm"}.
- `test_response_has_silent_scheduling`: Call `_voice_build_tool_response("call-123", "google_calendar", {"result": "Event at 3pm"})`. Assert the returned dict contains `"scheduling"` key with value `"SILENT"` (prevents Gemini from double-speaking the raw result before integrating it naturally).

Also add a test to the existing Gemini config test class:
- `test_config_includes_tools_when_provided`: Call `_gemini_build_config(tools=[{"name": "test", "description": "test tool", "parameters": {"type": "OBJECT", "properties": {}}}])`. Assert `config["config"]["tools"]` equals `[{"functionDeclarations": [...]}]`.
- `test_config_no_tools_when_none`: Call `_gemini_build_config()`. Assert `"tools"` not in `config["config"]`.

Run: `cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_voice.py -x -q`
Tests MUST fail (RED phase).
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_voice.py -x -q 2>&1 | tail -5</automated>
    <manual>Tests should fail with ImportError or AttributeError (functions don't exist yet)</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All new test classes exist in test_voice.py and fail because the implementation functions don't exist yet</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Implement _discover_voice_tools, _voice_execute_tool, _voice_build_tool_response, update _gemini_build_config</name>
  <files>docker/gateway.py</files>
  <action>
Add three new functions and modify one existing function in gateway.py. Place them right before `_gemini_connect()` (around line 9054), after `_voice_parse_server_message`.

**1. `_discover_voice_tools()` — returns `(declarations, name_map)`:**
```python
def _discover_voice_tools():
    """Query goosed for enabled extensions, return (Gemini function declarations, name_map).

    Returns:
        tuple: (list of function declaration dicts, dict mapping sanitized_name -> original_name)
    """
```
- Use `_goosed_conn(timeout=5)` + GET /config with X-Secret-Key header (follow pattern at line 6847)
- Parse `cfg.get("config", {}).get("extensions", {})`
- For each extension that is a dict and has `enabled != False`:
  - Sanitize name: `re.sub(r'[^a-zA-Z0-9_]', '_', ext_name)`. If starts with digit, prepend `ext_`.
  - Build declaration: `{"name": safe_name, "description": f"Use the {ext_name} extension/tool", "parameters": {"type": "OBJECT", "properties": {"request": {"type": "STRING", "description": "What to do with this tool"}}, "required": ["request"]}}`
  - Add `safe_name -> ext_name` to name_map
- On ANY exception: log warning via `_voice_log.warning(f"Tool discovery failed: {e}")`, return `([], {})`
- On non-200: close conn, return `([], {})`

**2. `_voice_execute_tool(tool_name, tool_args, session_id, original_name)` — returns dict:**
```python
def _voice_execute_tool(tool_name, tool_args, session_id, original_name):
    """Execute a tool call via goosed and return result dict."""
```
- Extract `request = tool_args.get("request", str(tool_args))`
- Build prompt: `f"Use the {original_name} tool: {request}"`
- Call `_do_rest_relay(prompt, session_id, timeout=15)`
- On success (no error): return `{"result": response_text[:2000]}`
- On error: return `{"error": error_string}`
- On exception: return `{"error": str(e)}`

**3. `_voice_build_tool_response(call_id, call_name, result)` — returns dict:**
```python
def _voice_build_tool_response(call_id, call_name, result):
    """Build Gemini toolResponse message dict."""
```
- Return: `{"toolResponse": {"functionResponses": [{"id": call_id, "name": call_name, "response": result}]}, "scheduling": "SILENT"}`
- The `"scheduling": "SILENT"` field is critical (TOOL-05): it prevents Gemini from immediately narrating the raw tool response. Instead, Gemini incorporates the result silently and speaks about it naturally on the next turn.

**4. Modify `_gemini_build_config` (line 8966):**
- Add `tools=None` parameter to function signature
- After the config dict is built (before return), add:
  ```python
  if tools:
      config["config"]["tools"] = [{"functionDeclarations": tools}]
  ```

Ensure `re` is already imported at the top of gateway.py (it is — verify with grep).

Run: `cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_voice.py -x -q`
All tests MUST pass (GREEN phase).
  </action>
  <verify>
    <automated>cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_voice.py -x -q</automated>
    <manual>All tests pass, including the new TestToolDiscovery, TestToolExecution, TestToolResponse classes</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All tests in test_voice.py pass. _discover_voice_tools returns correct declarations and name map. _voice_execute_tool relays to goosed correctly. _voice_build_tool_response produces valid Gemini JSON. _gemini_build_config includes tools when provided.</done>
</task>

</tasks>

<verification>
- `cd /Users/haseeb/nix-template/docker && python -m pytest tests/test_voice.py -x -q` — all tests green
- `cd /Users/haseeb/nix-template/docker && python -m pytest tests/ -x -q --ignore=tests/e2e` — full suite green
- Verify imports: `python -c "from gateway import _discover_voice_tools, _voice_execute_tool, _voice_build_tool_response"` works
</verification>

<success_criteria>
- _discover_voice_tools() parses goosed /config and returns Gemini function declarations for all enabled extensions
- Extension names are sanitized (hyphens, dots, spaces → underscores) with a reverse mapping for routing
- _voice_execute_tool() routes tool requests through goosed _do_rest_relay with 15s timeout
- _voice_build_tool_response() builds correct Gemini toolResponse format with SILENT scheduling (TOOL-05)
- _gemini_build_config() optionally includes tools in setup config
- All edge cases covered: empty config, disabled extensions, goosed errors, long results truncated
</success_criteria>

<output>
After completion, create `.planning/phases/32-tool-calling/32-01-SUMMARY.md`
</output>
